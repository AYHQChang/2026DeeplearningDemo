# GitHub 项目在 Linux 服务器上的拉取与更新

> 本文按“教师同步 GitHub → 发布到公共仓库 → 各账号克隆与更新”的顺序记录。
> 主流程已于 2026-09-02 由教师账号和独立测试账号验证跑通；后续每次发布仍需核对实际命令结果。
> 公开笔记不记录私人账号、服务器地址和连接端口；个人目录名统一使用 `xxx`，实际信息由课堂提供。
> 命令在已经连接服务器的 Linux Bash 终端执行，不是在 Windows CMD 中执行。采用兼容 Git 1.8.3 的写法。

## 1. 先明确：哪个版本为准，在哪里运行

正式课程版本以 [GitHub 仓库的 main 分支](https://github.com/AYHQChang/2026DeeplearningDemo) 为准。

```text
教师本地电脑：修改、测试、提交并 push
                      ↓
GitHub main：正式课程版本
                      ↓ fetch + reset
教师服务器工作副本：丢弃临时修改，对齐 GitHub
                      ↓ push
服务器公共发布仓库：只负责分发
                      ↓ clone / pull
各账号自己的项目：运行 Notebook，保存个人实验
```

各账号从公共仓库克隆后，不再需要通过外网下载课程代码。教师更新 GitHub 后，还必须在服务器执行同步和发布；公共仓库不会自动更新。

### 1.1 本文使用的路径

| 用途 | 路径或地址 | 说明 |
|---|---|---|
| GitHub 来源 | `https://github.com/AYHQChang/2026DeeplearningDemo.git` | 教师工作副本的 origin |
| 教师服务器工作副本 | `$HOME/2026DeeplearningDemo` | 在教师账号下使用 |
| 公共发布仓库 | `/data/public/xxx/course-repos/Deeplearning2026` | xxx 替换为课堂提供的目录名 |
| 各账号自己的项目 | `$HOME/courses/Deeplearning2026` | 在各自账号下使用 |
| 默认分支 | `main` | 同步和发布均使用此分支 |

`$HOME` 会自动指向当前登录账号的主目录，不要替换为教师的私人路径，也不要重新给 `HOME` 赋值。

`2026DeeplearningDemo` 和 `Deeplearning2026` 是本流程选用的不同目录名，不代表两个不同项目。执行 `cd` 时注意对应角色。

下面用一个变量缩短公共路径。每次新开终端或切换账号后，都需要重新设置：

```bash
# 先把 xxx 替换为课堂提供的实际目录名，再执行
COURSE_REPO="/data/public/xxx/course-repos/Deeplearning2026"

# 检查路径；输出不应再包含 xxx
echo "$COURSE_REPO"
```

### 1.2 操作约定

- 分段执行，不要一次性运行整篇文档。
- `cd` 或任何 Git 命令报错时，先停下，不继续执行后续命令。
- `&&` 表示“前一条成功，才执行后一条”，不要随意去掉。
- 公共仓库只用于分发，不在里面运行 Notebook、保存作业或安装 Conda。
- 教师的强制覆盖流程不适用于需要保留个人实验修改的账号。

## 2. 教师：不要服务器修改，以 GitHub 为准同步

在教师账号的服务器终端执行。本节假设工作副本已经存在；尚未克隆时，先完成第 7.1 节。

### 2.1 确认账号、目录、分支和来源

```bash
# 确认当前登录账号
whoami

# 进入教师已有的服务器工作副本
cd "$HOME/2026DeeplearningDemo"

# 确认当前 Git 仓库的根目录
git rev-parse --show-toplevel

# 确认当前分支
git rev-parse --abbrev-ref HEAD

# 查看远程来源
git remote -v

# 查看本地修改和分支状态
git status --short --branch
```

继续前应确认：

- 当前目录确实是教师的服务器工作副本，而不是公共仓库或其他人的项目。
- 分支是 `main`。
- `origin` 指向上述 GitHub 仓库，而不是公共文件夹。
- 所有要保留的正式修改都已经在本地电脑提交并推送到 GitHub。
- 当前服务器副本的本地修改确实不需要。

如果分支、目录或 origin 不符合预期，先排查，不要直接执行 reset。

### 2.2 下载成功后，再覆盖服务器副本

> 注意：以下命令会丢弃已跟踪文件的未提交修改，包括 Notebook 的代码、输出和元数据改动；当前分支上尚未发布到 origin/main 的本地提交也不再保留在该分支上。需要的内容必须提前另存。

```bash
# 最多等待 90 秒；只有 fetch 成功后才执行强制对齐
timeout 90 git fetch origin && git reset --hard origin/main

# 紧接着查看上一条命令的退出码，不要在中间插入其他命令
echo $?
```

命令含义：

- `git fetch origin`：下载 GitHub 提交并更新远程跟踪分支，不直接改动当前工作文件。
- `timeout 90`：限制等待时间，避免网络异常时一直转圈。
- `git reset --hard origin/main`：将当前分支、暂存区和已跟踪的工作文件对齐到刚获取的 `origin/main`。
- 退出码 `0`：整条同步命令成功。
- 退出码 `124`：被 timeout 限时终止。
- 其他非零退出码：同步失败，应阅读前面的错误信息。

正常完成时通常能看到 `HEAD is now at ...`。不能把“长时间没有输出”理解为已经成功。

这里使用 `git fetch origin`，不简化成 `git fetch origin main`，以避免旧版 Git 下仅更新 FETCH_HEAD、远程跟踪分支未按预期刷新的问题。

**fetch 超时或失败时，不要单独补跑 reset，也不要继续发布。** 本地已有的 `origin/main` 可能是缓存的旧提交。

关于“完全一致”的范围：reset 对齐的是提交和已跟踪文件，不会批量清理所有额外文件；但未跟踪文件如果挡住目标版本的写入，也可能被移除。不要依赖 reset 保留重要未跟踪文件。本流程不附加 `git clean`，也不删除环境、数据集或其他目录。

### 2.3 核对同步结果并记下提交号

```bash
# 查看状态；没有修改记录时，通常只显示分支行
git status --short --branch

# 查看当前版本的简短说明
git log -1 --oneline

# 记下完整提交号，后面与公共仓库和个人副本对照
git rev-parse HEAD

# 对比本地 main 与刚获取的 origin/main
git rev-list --left-right --count main...origin/main
```

最后一条预期输出：

```text
0    0
```

这表示本地 `main` 与已获取的 `origin/main` 没有差异。只有刚才 fetch 成功，这个比较才可用于判断本次同步结果；它本身不访问 GitHub。

如需再次查询 GitHub 此刻的分支指向，可以执行：

```bash
# 只读查询 GitHub main 的提交号，不更新工作文件
timeout 30 git ls-remote origin refs/heads/main
```

该提交号应与本地 HEAD 一致。若此查询超时，只能说明这次查询未完成，不能据此宣称已核验 GitHub 的最新状态。

## 3. 教师：把同步好的版本发布到公共文件夹

公共路径使用“裸仓库”，即只保存 Git 提交数据、不展开代码文件的发布仓库。各账号克隆之后才会得到普通项目文件。

### 3.1 首次建立公共仓库：只做一次

已经建好并验证成功的仓库，直接跳到第 3.2 节，不重复初始化。

父目录 `/data/public/xxx/course-repos` 必须已经存在且教师可写。目录或权限有问题时，按第 8 节处理，不开放私人主目录。

```bash
# 替换 xxx；新终端中需要重新设置
COURSE_REPO="/data/public/xxx/course-repos/Deeplearning2026"

# 检查父目录权限
ls -ld /data/public/xxx /data/public/xxx/course-repos

# 检查父目录中是否已经存在同名项目
ls -la /data/public/xxx/course-repos
```

如果 `Deeplearning2026` 已经存在且非空，先确认其用途，不删除、不覆盖，也不直接把已有普通仓库改成裸仓库。

确认目标尚未建立后：

```bash
# 自动创建目标目录，初始化为只供发布的裸仓库
git init --bare --shared=0644 "$COURSE_REPO" &&
git --git-dir="$COURSE_REPO" symbolic-ref HEAD refs/heads/main
```

- `--bare`：不创建工作副本，用来接收发布和提供克隆。
- `--shared=0644`：让 Git 创建的仓库文件由所有者写入，其他账号可读；目录获得相应的进入权限。
- `symbolic-ref`：将默认分支指定为 `main`，兼容不支持 `git init -b main` 的旧版 Git。

目录名称不必以 `.git` 结尾。本流程的名字就是 `Deeplearning2026`，后续命令必须保持一致。[Git init 官方说明](https://git-scm.com/docs/git-init)

### 3.2 发布 main 分支：以后每次更新都要做

先完成第 2 节并确认同步成功。在教师终端执行：

```bash
# 替换 xxx，明确这次发布的目标
COURSE_REPO="/data/public/xxx/course-repos/Deeplearning2026"

# 进入教师工作副本，成功后将 main 发布到公共仓库
cd "$HOME/2026DeeplearningDemo" &&
git push "$COURSE_REPO" main:refs/heads/main
```

这里的 `push` 目标是服务器公共路径，不是 GitHub：

- 第一次发布，通常出现 `[new branch] main -> main`。
- 后续有新提交时，显示提交范围更新。
- 没有新提交时，通常显示 `Everything up-to-date`。
- 如果出现 `rejected` 或 `non-fast-forward`，先停止排查，不添加 `--force`。

此操作发布 `main` 及其可达的提交历史，不复制整个主目录、GitHub 凭据、Conda 环境或未提交文件。历史中曾提交过的内容也可能被克隆，因此不能把“当前文件里已删除”当成“历史中已彻底清除”。

### 3.3 核对发布版本

```bash
# 教师工作副本的 main
git rev-parse main

# 公共仓库的 main
git --git-dir="$COURSE_REPO" rev-parse main

# 查看公共仓库最近一次提交的说明
git --git-dir="$COURSE_REPO" log -1 --oneline main
```

前两个提交号应完全相同。至此可以确认这次工作副本的版本已发布。

在公共目录中看到 `HEAD`、`objects`、`refs`、`config` 等内容，而没有 Notebook，是裸仓库的正常结构。不要在里面手动复制代码，也不要对它运行普通的 `git pull`。

## 4. 各账号：从公共仓库克隆到自己的目录

使用自己的账号连接同一台服务器，打开自己的终端。此处的“服务器本地克隆”指服务器内部磁盘上的仓库，不是 Windows 电脑上的文件夹。

### 4.1 确认账号和公共路径

```bash
# 查看当前账号和个人主目录
whoami
echo "$HOME"

# 查看实际生效的组身份
id

# 替换 xxx，设置课堂提供的公共仓库路径
COURSE_REPO="/data/public/xxx/course-repos/Deeplearning2026"
```

不使用教师账号，不使用教师的主目录。普通课堂账号无需加入具有公共目录写权限的教师组。

### 4.2 首次测试读取权限

```bash
# 只读查询公共仓库的 main，不下载完整项目
git ls-remote "$COURSE_REPO" refs/heads/main

# 检查当前账号是否能写公共课程目录中的内容
find /data/public/xxx -writable -print
```

预期结果：

- 第一条输出“一串提交号 + refs/heads/main”，提交号与教师发布时一致。
- 第二条没有输出，也没有报错。如果列出路径，说明存在可写内容，应先检查权限，不通过实际修改公共文件来测试。

上述 find 检查不包含 `/data/public` 自身。管理员还需保证普通账号不能通过可写的父目录移动、替换或删除教师公共目录；“仓库内部只读”不能抵消父目录写权限带来的风险。

### 4.3 第一次克隆

```bash
# 创建自己的课程目录，成功后开始克隆
mkdir -p "$HOME/courses" &&
git clone --no-local -b main "$COURSE_REPO" "$HOME/courses/Deeplearning2026"
```

- `--no-local`：关闭本地硬链接式克隆优化，使用 Git 传输方式读取，适合跨账号克隆。
- `-b main`：克隆完成后使用 `main` 分支。
- 第一个路径是公共来源，第二个路径是自己的目标目录。
- 来源是同一台服务器的绝对路径，不需要访问 GitHub，也不需要教师密码。[Git clone 官方说明](https://git-scm.com/docs/git-clone)

如果提示目标目录已经存在且非空，先按第 9.1 节检查，不删除已有实验内容。

### 4.4 检查项目是否完整到位

```bash
# 进入自己的工作副本
cd "$HOME/courses/Deeplearning2026"

# 查看代码和笔记文件
ls

# 查看更新来源
git remote -v

# 查看分支和修改状态
git status --short --branch

# 查看当前提交号
git rev-parse HEAD
```

检查标准：

- 能看到 README、实验目录等项目文件。
- `origin` 指向公共仓库路径，而不是 GitHub 或教师私人主目录。
- 当前分支为 `main`，没有未提交修改。
- HEAD 与教师发布的提交号一致。

`git remote -v` 中同时出现 `(fetch)` 和 `(push)` 只是地址配置，不代表获得了公共仓库写权限。

VS Code 中打开 `$HOME/courses/Deeplearning2026` 对应的实际目录，再选择自己的 Python 环境运行 Notebook。Git 克隆只带来项目内容，不会自动安装 Conda、PyTorch 或 Notebook 内核。

### 4.5 已经克隆过：直接更新

没有需要处理的本地修改时：

```bash
# 进入成功后，从公共仓库更新
cd "$HOME/courses/Deeplearning2026" &&
git pull --ff-only origin main
```

`--ff-only` 只允许快进更新，不自动生成合并提交。如果历史已经分叉，会停止而不是自动合并。

没有新版本时，旧版 Git 可能显示 `Already up-to-date.`，新版可能显示 `Already up to date.`，都表示相同含义。刚克隆完再次 pull 出现这个提示是正常的。

### 4.6 删除个人项目并重新克隆

适用于个人项目经过多次修改、克隆或测试后已经混乱，且确认项目内的内容全部不再需要，希望从公共版本重新开始的情况。

本节的“重新开始”只指删除并重建这一个个人工作副本：

```text
$HOME/courses/Deeplearning2026
```

不是删除 Linux 账号，也不是清空整个主目录。只在自己的普通账号下执行，不使用教师账号、root 或 sudo。

#### 4.6.1 删除范围和准备事项

> 删除不会进入回收站。项目内的代码、Notebook 输出、数据、隐藏的 .git、stash 和未发布的本地提交都在删除范围内。重新克隆只能恢复公共仓库中已有的版本，不能恢复尚未发布的个人修改。

- 先停止这个项目中正在运行的 Notebook 内核和训练任务，关闭编辑该项目的窗口，避免后台继续写文件。
- 如果有任何不确定是否需要的内容，先通过 Xftp 下载备份；需要保留 Git 历史时，也要包含隐藏的 `.git`。
- 项目目录外的 Conda 环境、`.ssh`、其他项目及公共发布仓库不在删除范围内；如果把环境或数据放在本项目内部，它们也会被删除。
- 本节不自动清理其他位置的旧副本，例如 `$HOME/2026DeeplearningDemo`。其他目录需要逐个确认，不能用通配符批量删除。
- 不对符号链接或挂载目录套用此流程。遇到特殊目录、权限错误或与下文不一致的路径，先停下排查，不扩大删除范围。

#### 4.6.2 先确认公共来源可以读取

在自己的服务器终端执行：

```bash
# 确认当前账号和主目录
whoami
echo "$HOME"

# xxx 替换为课堂提供的实际公共目录名
COURSE_REPO="/data/public/xxx/course-repos/Deeplearning2026"

# 删除前先确认公共版本可读取
git ls-remote "$COURSE_REPO" refs/heads/main
```

只有成功输出“一串提交号 + refs/heads/main”后才继续。公共仓库不可读时，不要先删掉仍可使用的个人副本。

#### 4.6.3 检查要删除的确切目录

```bash
# 显示个人主目录的实际路径
readlink -f "$HOME"

# 显示目标目录的实际路径，检查是否被符号链接指向其他位置
readlink -f "$HOME/courses/Deeplearning2026"

# 查看目录所有者以及全部内容，包括隐藏文件
ls -ld "$HOME/courses/Deeplearning2026"
ls -la "$HOME/courses/Deeplearning2026"
```

目标实际路径必须是“本人主目录的实际路径 + /courses/Deeplearning2026”，且目录所有者应为本人。不能是主目录本身、整个 courses 目录、公共仓库或其他账号的路径。

如果项目已经不存在，就没有必要删除，直接进入第 4.6.5 节重新克隆。仅看见一个目录名相似，不代表可以删除它。

#### 4.6.4 确认全部不要后，再删除

完整复制下面的代码块，不要单独摘出 rm 命令，也不要改成删除整个主目录：

```bash
# 回到自己的主目录，避免终端停留在待删除的项目中
cd "$HOME" &&
if [ -d "$HOME/courses/Deeplearning2026" ] &&
   [ -O "$HOME/courses/Deeplearning2026" ] &&
   [ "$(readlink -f "$HOME/courses/Deeplearning2026")" = "$(readlink -f "$HOME")/courses/Deeplearning2026" ]; then
    # 仅删除这个个人项目目录；大写 I 会要求确认
    rm -rI --one-file-system -- "$HOME/courses/Deeplearning2026"
else
    echo "停止：项目不存在、所有者不是当前账号，或实际路径不符合预期；未执行删除。"
fi
```

这段代码先检查目录存在、所有者为当前账号、实际路径符合预期，三项满足后才进入删除步骤。

- `-r`：递归删除指定目录及其中内容，包括隐藏文件。
- `-I`：大写字母 I，递归删除前询问是否继续；确实要删才输入 `y`，取消输入 `n`。
- `--one-file-system`：跳过内部位于其他文件系统的目录；它不是所有挂载场景的安全保证，仍需完成前面的路径检查。
- `--`：后面的参数作为路径处理。[GNU rm 官方说明](https://www.gnu.org/s/coreutils/manual/html_node/rm-invocation.html)

如果取消确认、出现 Permission denied 或提示跳过目录，不要加 sudo、不要去掉保护选项。先检查剩余内容和原因。

删除后执行只读检查：

```bash
# 既不存在文件/目录，也不存在残留的符号链接，才可以重新克隆
if [ ! -e "$HOME/courses/Deeplearning2026" ] &&
   [ ! -L "$HOME/courses/Deeplearning2026" ]; then
    echo "个人项目目录已不存在，可以重新克隆。"
else
    echo "停止：目标仍存在，请先检查；不要重复强制删除。"
fi
```

不要只看 rm 的退出码判断目录是否已删掉：取消确认也可能返回成功状态，最终要检查目标是否仍存在。

#### 4.6.5 重新克隆，并验证来源

确认原目标目录已不存在后：

```bash
# 如果换过终端，需要重新设置；xxx 仍须替换
COURSE_REPO="/data/public/xxx/course-repos/Deeplearning2026"

# 只创建上一级 courses；最终项目目录由 Git 自动创建
mkdir -p "$HOME/courses" &&
git clone --no-local -b main "$COURSE_REPO" "$HOME/courses/Deeplearning2026"
```

克隆成功后：

```bash
# 进入全新的个人副本，检查来源、状态和版本
cd "$HOME/courses/Deeplearning2026" &&
git remote -v &&
git status --short --branch &&
git rev-parse HEAD &&
git ls-remote "$COURSE_REPO" refs/heads/main
```

origin 应指向公共仓库，当前分支应为 main，状态中不应列出个人修改；最后两条的提交号应一致（核对期间公共分支未再次更新时）。

重新打开自己的项目，选择原先已经安装的 Python/Conda 环境即可。只要环境位于本次删除目录之外，就不需要因为重新克隆项目而重装环境。

## 5. 日常操作速查：建好以后主要用这两组命令

### 5.1 教师：同步 GitHub，然后发布公共仓库

前提：本地电脑已经 push；服务器工作副本为 `main`；`origin` 指向 GitHub；服务器修改明确不要；公共裸仓库已经建好。

```bash
# 替换 xxx；每次新终端重新设置
COURSE_REPO="/data/public/xxx/course-repos/Deeplearning2026"

# 任一步失败即停止，不继续覆盖或发布
cd "$HOME/2026DeeplearningDemo" &&
timeout 90 git fetch origin &&
git reset --hard origin/main &&
git push "$COURSE_REPO" main:refs/heads/main

# 必须紧接上面命令查看：0 才表示整条流程成功
echo $?
```

整条流程成功后核对：

```bash
# 两个提交号应相同
git rev-parse HEAD
git --git-dir="$COURSE_REPO" rev-parse main
```

如果 reset 成功但 push 失败，说明教师副本已更新、公共仓库尚未确认更新；修复发布问题后重新 push，不要通知其他账号“公共版本已更新”。

### 5.2 各账号：更新自己的项目

```bash
# 先检查是否有个人修改
cd "$HOME/courses/Deeplearning2026" &&
git status --short --branch
```

确认没有需要处理的修改后：

```bash
# 更新成功后显示最新提交说明
git pull --ff-only origin main &&
git log -1 --oneline
```

第一次用 clone，以后用 pull。不需要每次重新克隆，也不需要每个人连接 GitHub。

## 6. 个人项目已经修改，如何获取公共更新

运行 Notebook 也可能改变输出和元数据，因此即使没有手动改代码，Git 仍可能报告修改。

先确认自己在个人工作副本中：

```bash
cd "$HOME/courses/Deeplearning2026" &&
git status --short --branch
```

### 6.1 修改需要保留：先暂存，再更新，再恢复

建议先保存正在编辑的 Notebook。然后执行兼容旧版 Git 的 stash 写法：

```bash
# 保存已跟踪文件的修改和未跟踪文件
git stash save -u "before-course-update"

# 确认本次确实创建了新的 stash
git stash list
```

确认出现本次新建的 `before-course-update` 记录后再继续。如果提示 `No local changes to save`，说明本次没有新建 stash，更新后不要误恢复以前的旧记录。

```bash
# 拉取公共更新
git pull --ff-only origin main
```

只有 pull 成功，且刚才确实保存了新 stash、期间没有再创建其他 stash 时，才恢复：

```bash
# 恢复最新保存的修改，但保留 stash 记录，便于核对
git stash apply

# 查看恢复结果
git status --short
```

`-u` 不包含被 .gitignore 忽略的文件；重要数据和实验结果仍应另行备份。

如果 apply 报冲突，不反复执行 apply 或 pull。先用 `git status` 查看冲突文件，再逐个处理；Notebook 的 JSON 冲突不宜直接批量覆盖。本流程暂不删除 stash 备份。

如果个人分支已有提交，且与公共分支分叉，stash 不能消除提交历史分叉；应保留现场，再选择合并或另建副本。

### 6.2 修改完全不需要：覆盖为公共版本

本节仅用于本人明确放弃本地改动的个人副本，不默认用于所有账号。

```bash
# 再次确认位置、分支和来源
pwd
git rev-parse --abbrev-ref HEAD
git remote -v
git status --short --branch
```

确认当前是个人项目、分支为 main、origin 指向课堂公共仓库后：

```bash
# 读取公共最新版本成功后，才丢弃本地改动
git fetch origin && git reset --hard origin/main
```

影响与第 2.2 节相同：已跟踪修改会被覆盖，当前分支会移动到 origin/main；不要依赖此命令保留重要未跟踪文件。

此处对齐的是“公共仓库版本”，不是直接连接 GitHub。公共仓库是否最新，取决于教师是否完成同步和发布。

## 7. 补充：直接从 GitHub 云端克隆

用于教师首次准备工作副本，或演示标准 GitHub 克隆。正式课堂默认使用第 4 节的服务器本地克隆。

### 7.1 教师第一次准备服务器工作副本

目标目录必须不存在，或确实为空。已有项目请回到第 2 节更新，不重复克隆。

```bash
# 下载 GitHub main，最多等待 180 秒
timeout 180 git clone -b main https://github.com/AYHQChang/2026DeeplearningDemo.git "$HOME/2026DeeplearningDemo"
```

成功后回到第 2.1 节检查来源和分支，再建立并发布公共仓库。

### 7.2 各账号演示云端克隆

使用独立目录名，不覆盖已从公共仓库克隆的课堂项目：

```bash
# 直接访问 GitHub，保存为独立的演示副本
mkdir -p "$HOME/courses" &&
timeout 180 git clone -b main https://github.com/AYHQChang/2026DeeplearningDemo.git "$HOME/courses/Deeplearning2026-github"
```

完成后：

```bash
cd "$HOME/courses/Deeplearning2026-github" &&
git remote -v
```

这个副本的 origin 是 GitHub。以后在该副本中执行：

```bash
# 无本地修改时，从 GitHub 更新这个演示副本
timeout 90 git pull --ff-only origin main
```

云端克隆需要服务器能访问 GitHub；公共仓库克隆只需要服务器内部路径可读。克隆课程公开仓库通常不需要 GitHub 登录凭据，不应输入教师的 Token、密码或私人密钥。

## 8. 补充：公共目录首次准备与权限检查

本节仅用于教师首次准备公共父目录。已经跑通的环境不需要重复创建或修改权限。

### 8.1 检查目录与组身份

```bash
# 查看公共根目录的权限和所属组
ls -ld /data /data/public

# 当前终端实际使用的组身份
id

# 系统记录的当前账号组成员信息
id "$(whoami)"
```

例如 /data/public 的所有者为 root、所属组为 teachers、权限为 775 时，普通用户要通过组权限在其中建目录，需要当前终端已经拥有 teachers 身份。

如果系统记录已包含 teachers，而当前 `id` 尚未包含，说明旧登录会话尚未更新。可以重新建立 SSH 登录会话，或在当前终端执行：

```bash
# 仅在管理员已加入该组、且实际组名确实是 teachers 时使用
newgrp teachers

# 检查新 Shell 的有效身份
id
```

newgrp 启动一个新 Shell，不会刷新所有已打开的 VS Code 终端。如果要求未知密码或报错，先停下联系管理员，不尝试放宽私人目录权限。

### 8.2 依次创建父目录

把 xxx 替换为实际目录名。仅在对应目录尚不存在时执行：

```bash
# 先建教师自己的公共分类目录，成功后再建下一级
mkdir -m 755 /data/public/xxx &&
mkdir -m 755 /data/public/xxx/course-repos

# 检查所有者和权限
ls -ld /data/public/xxx /data/public/xxx/course-repos
```

如果第一层已存在、第二层不存在，只执行第二条 mkdir，不重复执行整组命令。

755 表示所有者可读写和进入，组内及其他账号可读取和进入，但不能在该目录中新增、删除条目。实际访问还受父目录、ACL 等影响，最终用第 4.2 节的独立账号测试确认。

`No such file or directory` 表示某个父目录缺失，不代表权限已经正常。`Permission denied` 表示访问被拒绝，需要检查当前身份和目录权限。

不要递归放开教师主目录，不使用 777。教师只维护公共课程目录；普通账号只读公共仓库，并在各自目录中写入实验内容。

## 9. 常见问题与停止条件

### 9.1 目标目录已经存在

`destination path ... already exists and is not an empty directory` 表示目标非空，不是要求删除它。

先检查已有内容：

```bash
ls -la "$HOME/courses/Deeplearning2026"
```

确认是已有课程项目后：

```bash
cd "$HOME/courses/Deeplearning2026" &&
git status --short --branch &&
git remote -v
```

根据来源和本地修改决定更新方式。若只是一次失败克隆留下的目录或非 Git 目录，先确认内容，再选择另一个新的目标目录，不直接删除。

### 9.2 公共仓库无法读取

出现 `does not appear to be a git repository` 或 `Could not read from remote repository` 时，可能是路径错误、不是 Git 仓库，也可能是父目录或仓库不可读。

检查 xxx 是否已替换、路径大小写是否一致，以及教师是否已初始化并发布 main。不要把公共路径改成教师私人工作目录来绕过问题。

### 9.3 GitHub 超时

`Failed connect to github.com` 或 `Operation timed out` 表示本次 GitHub 连接没有完成，不能仅凭这些信息确定具体是网络、代理还是防火墙原因。

如果下面的教师同步命令结束后，紧接着执行 `echo $?` 得到 `124`：

```bash
timeout 90 git fetch origin && git reset --hard origin/main
```

它表示 `fetch` 超过 90 秒，被 `timeout` 终止。由于中间使用了 `&&`，后面的 `reset --hard` 没有执行，教师工作副本和公共仓库均未完成本次更新。这是安全停止状态。

此时不要单独执行 `git reset --hard origin/main`，也不要把当前版本继续发布到公共仓库。已有的 `origin/main` 可能只是以前缓存的版本。

#### 9.3.1 先测试连接

在教师服务器工作副本中执行：

```bash
cd "$HOME/2026DeeplearningDemo"

# 测试普通 HTTPS 连接
curl -I --connect-timeout 10 --max-time 30 https://github.com

# 必须紧接上一条命令查看退出码
echo $?
```

curl 返回 HTTP 响应且退出码为 `0`，只说明此刻能够建立基本 HTTPS 连接，不保证 Git 下载一定成功。继续进行 Git 的只读测试：

```bash
# 只查询 GitHub main 的提交号，不改变本地项目
timeout 60 git ls-remote origin refs/heads/main

# 必须紧接上一条命令查看退出码
echo $?
```

成功时会输出“一串提交号 + `refs/heads/main`”，退出码为 `0`。如果仍然返回 `124`，说明本次 Git 查询依然超时，先停止同步，稍后重试。

#### 9.3.2 连接恢复后延长 fetch 时间

`git ls-remote` 成功后，可以将 fetch 的等待时间延长到 300 秒：

```bash
# 先只下载，不自动覆盖工作副本
timeout 300 git fetch origin

# 必须紧接上一条命令查看退出码
echo $?
```

- 退出码 `0`：fetch 成功，可以继续。
- 退出码 `124`：再次超时，不执行 reset，也不发布。
- 其他非零退出码：阅读 Git 给出的错误信息，排查后重试。

只有退出码为 `0` 时，才执行：

```bash
# 对齐刚刚成功获取的 GitHub main
git reset --hard origin/main

# 核对教师工作副本
git status --short --branch
git log -1 --oneline
git rev-list --left-right --count main...origin/main
```

最后一条预期输出 `0  0`。然后才能发布并核对公共版本：

```bash
# xxx 替换为课堂提供的公共目录名
COURSE_REPO="/data/public/xxx/course-repos/Deeplearning2026"

# 发布到公共仓库
git push "$COURSE_REPO" main:refs/heads/main

# 教师工作副本和公共仓库的提交号应完全相同
git rev-parse HEAD
git --git-dir="$COURSE_REPO" rev-parse main
```

#### 9.3.3 服务器持续无法访问 GitHub 时的备用路线

如果服务器长期无法主动访问 GitHub，但教师本地电脑可以同时访问 GitHub 和通过 SSH 连接服务器，可以从本地电脑直接发布到公共仓库。该方式绕过服务器向 GitHub 发起的连接。

先在教师本地项目目录的 PowerShell 中核对本地 `main` 与 GitHub 一致：

```powershell
git fetch origin
git status --short --branch
git rev-list --left-right --count main...origin/main
```

只有 fetch 成功、没有需要提交的修改，而且最后输出 `0  0` 时，才执行：

```powershell
# 使用本机 SSH config 中的教师连接别名；替换别名和 xxx
git push "<教师SSH别名>:/data/public/xxx/course-repos/Deeplearning2026" main:refs/heads/main
```

这会把本地已经核对过的 `main` 直接推到服务器公共仓库。不要在公开笔记中记录真实账号、IP、端口或密码；密码只在 SSH 提示时输入。

备用发布成功后，公共仓库可以供其他账号更新，但教师服务器的普通工作副本仍可能停留在旧版本。待服务器访问 GitHub 恢复后，再执行第 2 节的正常 `fetch → reset` 流程。

不通过关闭 SSL 证书验证处理超时，也不在失败后单独执行 reset 掩盖同步失败。

### 9.4 本地修改阻止更新

`Your local changes would be overwritten by merge` 表示需要先处理工作副本的修改：

- 教师发布副本明确不要修改：使用第 2 节。
- 个人实验需要保留：使用第 6.1 节。
- 个人实验明确不要：使用第 6.2 节。

### 9.5 公共发布被拒绝或版本没有变化

push 报 non-fast-forward 时不强推，先核对来源、目标和双方提交号。

pull 显示已是最新，但没有预期新内容时，先查看 `git remote -v`，再核对三处提交号：教师工作副本、公共仓库、个人副本。常见情况是只更新了 GitHub，却尚未更新教师副本或尚未 push 公共仓库。

### 9.6 VS Code 图形界面一直转圈

先看终端中的 Git 命令结果，不仅依赖版本图或旋转图标。旧版 Git 与较新的 VS Code 集成功能可能存在兼容问题，升级由管理员另行安排。

本流程使用旧版兼容命令，不要求 `git switch`、`git restore`、`git -C` 或 `git stash push`。

## 10. 完成标准与参考

本次分发成功应满足：

1. 教师 fetch 成功，工作副本已对齐 GitHub main。
2. 教师工作副本 main 与公共仓库 main 的提交号一致。
3. 独立课堂账号能只读查询、克隆公共仓库。
4. 个人项目 HEAD 与本次公共发布提交号一致。
5. 个人项目能再次 pull；个人实验保存在自己的目录中。

“克隆成功”不等于 Python 环境已经安装成功；运行实验前仍需按环境安装笔记准备依赖。

参考资料：

- [课程 GitHub 仓库](https://github.com/AYHQChang/2026DeeplearningDemo)
- [Git init：裸仓库与共享权限](https://git-scm.com/docs/git-init)
- [Git clone：克隆与 --no-local](https://git-scm.com/docs/git-clone)
- [Git reset：强制对齐的影响范围](https://git-scm.com/docs/git-reset)
- [Git stash：保存和恢复工作修改](https://git-scm.com/docs/git-stash)
