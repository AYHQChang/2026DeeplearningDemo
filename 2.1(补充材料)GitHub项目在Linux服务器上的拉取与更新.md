# GitHub 项目在 Linux 服务器上的拉取与更新

本文记录如何通过个人 SSH 账号登录课程 Linux 服务器，将 GitHub 上的深度学习课堂项目首次拉取到服务器，并在课程仓库更新后同步最新代码。内容以只读拉取为主，不在共享账号中配置仓库管理者的 GitHub 凭据。

## 1. 项目信息

| 项目 | 当前值 |
|---|---|
| GitHub 仓库 | `https://github.com/AYHQChang/2026DeeplearningDemo.git` |
| 仓库网页 | `https://github.com/AYHQChang/2026DeeplearningDemo` |
| 仓库可见性 | Public，公开仓库 |
| 默认分支 | `main` |
| Linux 服务器 | `172.23.168.7` |
| SSH 端口 | `22` |
| Linux 用户名 | `<你的 Linux 用户名>` |
| 本机 SSH 别名 | `ayit-student` |
| 建议服务器目录 | `~/courses/2026DeeplearningDemo` |

本文编写时，GitHub 页面显示该仓库为公开仓库，默认分支为 `main`。公开仓库的 HTTPS `clone` 和 `pull` 不需要输入 GitHub 用户名、GitHub 密码或 Personal Access Token。

> 安全边界：服务器登录密码只用于 SSH 登录。GitHub 不接受把 Linux 密码当作 GitHub 密码。不要把仓库管理者的 GitHub Token、GitHub 密码或 SSH 私钥放到共享服务器账号中。

## 2. Git 中几个动作的区别

| 命令 | 作用 | 典型使用时机 |
|---|---|---|
| `git clone` | 从 GitHub 创建一份包含历史记录的完整本地副本 | 服务器第一次获取项目 |
| `git fetch` | 获取远端的新提交和分支信息，但不修改当前工作文件 | 查看远端是否更新 |
| `git pull` | 获取远端更新并合并到当前分支 | 后续同步教师发布的新版本 |
| `git status` | 查看当前分支和文件改动 | 执行更新前后检查 |
| `git add` | 将选定修改放入暂存区 | 准备本地提交 |
| `git commit` | 在本地记录一个版本 | 保存自己的练习修改 |
| `git push` | 将本地提交上传到 GitHub | 需要远端写权限和 GitHub 身份验证 |

对本课程服务器而言，最短主流程是：

```text
第一次：SSH 登录 → git clone → 检查仓库
以后：SSH 登录 → cd 进入仓库 → git status → git pull --ff-only
```

## 3. 第一次拉取前的检查

### 3.1 登录 Linux 服务器

在 Windows PowerShell 中执行：

```powershell
ssh ayit-student
```

输入你自己的账号密码。密码输入时不会显示字符、圆点或星号。

登录成功后，提示符类似：

```text
[your_username@ag-g-02 ~]$
```

从这一行开始，后续命令均在 Linux 服务器终端中执行。

### 3.2 检查 Git 是否安装

```bash
git --version
```

正常结果类似：

```text
git version 2.x.x
```

如果出现：

```text
bash: git: command not found
```

说明服务器没有安装 Git，或 Git 不在 `PATH` 中。普通账号通常没有系统软件安装权限，应将该结果交给服务器管理员处理，不建议自行安装系统软件。

### 3.3 检查服务器能否访问 GitHub

```bash
git ls-remote https://github.com/AYHQChang/2026DeeplearningDemo.git HEAD
```

如果网络和 Git 均正常，会返回一行提交哈希和 `HEAD`，例如：

```text
<commit-hash>    HEAD
```

该命令只读取远端信息，不会下载完整仓库，也不会修改服务器文件。

## 4. 第一次将项目克隆到服务器

### 4.1 创建课程父目录

```bash
mkdir -p ~/courses
cd ~/courses
pwd
```

在当前账号下，`~` 和 `$HOME` 都表示该账号自己的主目录。不同账号的完整路径可能不同，因此使用 `~` 或 `$HOME` 比写死个人绝对路径更安全。

### 4.2 确认目标目录尚不存在

```bash
ls -la
```

如果列表中没有 `2026DeeplearningDemo`，可以执行首次克隆：

```bash
git clone https://github.com/AYHQChang/2026DeeplearningDemo.git
```

Git 会创建：

```text
~/courses/2026DeeplearningDemo
```

完整仓库不仅包括当前文件，还包括 `.git` 目录中的版本历史和远端信息。

### 4.3 进入项目并核验

```bash
cd ~/courses/2026DeeplearningDemo
git status
git remote -v
git branch --show-current
git log -1 --oneline --decorate
```

预期关键结果：

```text
当前分支：main
远端名称：origin
远端地址：https://github.com/AYHQChang/2026DeeplearningDemo.git
工作区：没有未提交修改
```

也可以检查项目文件：

```bash
ls -la
```

应能看到 `README.md`、`requirements.txt`、`01_mlp_lab` 等内容。

## 5. 第一次拉取的完整命令清单

以下命令适用于确认目标目录尚不存在的情况：

```bash
git --version
git ls-remote https://github.com/AYHQChang/2026DeeplearningDemo.git HEAD
mkdir -p ~/courses
cd ~/courses
git clone https://github.com/AYHQChang/2026DeeplearningDemo.git
cd 2026DeeplearningDemo
git status
git remote -v
git branch --show-current
git log -1 --oneline --decorate
```

不要在项目目录内部再次执行同一个 `git clone`，否则容易形成重复嵌套目录或得到“目标目录已存在”的错误。

## 6. 教师更新 GitHub 后，服务器如何同步

后续不再执行 `git clone`。每次上课前可以执行：

```bash
cd ~/courses/2026DeeplearningDemo
git status --short
git branch --show-current
git pull --ff-only origin main
```

`--ff-only` 表示只允许快进更新。如果服务器工作区与 GitHub 已经产生分叉，Git 会停止并报告问题，而不是自动生成一个难以理解的合并提交。这种行为更适合课堂统一代码。

更新后核验：

```bash
git status
git log -1 --oneline --decorate
```

如果没有新版本，Git 通常显示：

```text
Already up to date.
```

## 7. 先查看远端更新，不立即修改工作文件

如果希望先检查 GitHub 是否有新提交：

```bash
cd ~/courses/2026DeeplearningDemo
git fetch origin
git status
git log --oneline --decorate HEAD..origin/main
```

说明：

- `git fetch origin` 更新远端跟踪信息。
- `HEAD..origin/main` 只列出远端已有而当前本地尚未包含的提交。
- 上述命令不会立即覆盖当前工作文件。

确认后再执行：

```bash
git pull --ff-only origin main
```

## 8. 需要修改练习代码时的建议

如果需要改参数、完成练习或保存自己的实验版本，不建议直接长期修改公共的 `main` 分支。可以在首次克隆后创建个人练习分支：

```bash
cd ~/courses/2026DeeplearningDemo
git switch -c practice-<user-id>
```

将 `<user-id>` 替换为约定的学号或唯一标识，不保留尖括号。例如：

```bash
git switch -c practice-20260001
```

修改后查看状态：

```bash
git status --short
```

如果课程要求用 Git 记录实验，可以只添加本次需要的文件：

```bash
git add 01_mlp_lab/具体文件名
git commit -m "Record MLP practice changes"
```

不建议初学阶段直接使用 `git add .`，因为它可能把日志、模型、数据或临时文件一起加入提交。

需要更新教师发布的 `main` 时：

```bash
git switch main
git pull --ff-only origin main
git switch practice-20260001
git merge main
```

如果最后一步出现冲突，应停止批量操作，查看 Git 标记的冲突文件，并在教师指导下处理。

## 9. 如果已经直接修改了 main

更新前先执行：

```bash
cd ~/courses/2026DeeplearningDemo
git status --short
```

如果有未提交修改，`git pull` 可能拒绝更新，或在合并时产生冲突。优先采用以下保留方式。

### 9.1 将现有修改保存到新分支

```bash
git switch -c practice-before-update
git add 需要保存的具体文件
git commit -m "Save practice before updating main"
git switch main
git pull --ff-only origin main
```

### 9.2 临时存放未提交修改

```bash
git stash push -u -m "Before course update"
git pull --ff-only origin main
git stash pop
```

`git stash pop` 可能产生冲突。执行前应确认哪些文件属于个人练习，且重要结果已经另行备份。

## 10. 多人使用服务器时的目录安排

如果每人有独立 Linux 账号，可以使用：

```text
~/courses/2026DeeplearningDemo
```

如果多人共用同一个 Linux 账号，不能共享同一份可写工作目录进行练习，否则不同用户的修改会互相覆盖。至少应按学号或唯一标识区分目录：

```bash
mkdir -p ~/courses/<user-id>
cd ~/courses/<user-id>
git clone https://github.com/AYHQChang/2026DeeplearningDemo.git
```

例如：

```text
~/courses/20260001/2026DeeplearningDemo
~/courses/20260002/2026DeeplearningDemo
```

这种目录区分只能减少误覆盖，不构成真正的账号权限隔离。多人课堂长期使用时，更合适的方案是由管理员分配独立 Linux 用户或独立容器环境。

## 11. 仓库公开与 GitHub 身份验证

### 11.1 只进行 clone 和 pull

当前仓库是公开仓库，以下操作不需要 GitHub 登录：

```bash
git clone https://github.com/AYHQChang/2026DeeplearningDemo.git
git pull --ff-only origin main
git fetch origin
```

如果公开仓库在 `clone` 时要求 GitHub 用户名和密码，应先检查远端 URL、代理设置或是否被重定向，而不是输入 Linux 登录密码。

### 11.2 从服务器 push 到教师仓库

`git push` 是写操作，需要 GitHub 仓库写权限以及 GitHub 身份验证。共享账号不应保存仓库管理者的 Token 或 SSH 私钥，因此课堂服务器建议只拉取，不直接向主仓库推送。

如果以后确实需要提交代码，可以选择：

1. 每人使用自己的 GitHub 账号并 fork 仓库。
2. 每人使用独立 Linux 账号和自己的 GitHub SSH key。
3. 使用学校指定的作业提交系统，而不是共享一个 GitHub 写入凭据。

## 12. 常见错误与处理

### 12.1 `git: command not found`

原因：服务器未安装 Git，或 Git 不在 `PATH`。

处理：记录错误并联系管理员安装 Git。普通账号不直接改动系统软件。

### 12.2 `fatal: destination path '2026DeeplearningDemo' already exists`

原因：目标目录已经存在。

先检查它是否已经是正确仓库：

```bash
cd ~/courses/2026DeeplearningDemo
git status
git remote -v
```

如果它是正确仓库，执行 `git pull --ff-only origin main`，不再重复克隆。

如果它不是 Git 仓库，不要直接删除。先将其中的重要文件备份或把目录改名，再重新克隆。

### 12.3 `Could not resolve host: github.com`

原因：服务器 DNS 无法解析 GitHub，或服务器没有可用的外网 DNS。

处理：联系网络或服务器管理员。反复修改 Git 用户名和密码不会解决 DNS 问题。

### 12.4 `Failed to connect to github.com port 443`

原因：服务器到 GitHub HTTPS 的网络路径、出口防火墙或代理配置存在问题。

处理：确认学校网络策略和服务器代理要求。不要随意复制来源不明的全局代理设置。

### 12.5 `SSL certificate problem`

旧 CentOS 服务器可能存在过期的 CA 证书、过旧的 Git/cURL 或 TLS 兼容问题。

处理：由管理员更新 `ca-certificates`、Git 或相关 TLS 组件。不要使用以下方式绕过证书验证：

```text
git config --global http.sslVerify false
```

关闭证书验证会失去对 GitHub 服务器身份的有效校验，不适合作为课堂配置。

### 12.6 `Your local changes would be overwritten by merge`

原因：服务器上有未提交修改，而 GitHub 更新将影响相同文件。

处理：先执行 `git status --short`，再按第 9 节保存到分支或 stash，不直接删除个人文件。

### 12.7 `Not possible to fast-forward, aborting`

原因：当前本地分支和远端分支已经分叉，`--ff-only` 为避免自动合并而停止。

处理：执行以下命令收集证据：

```bash
git status
git log --oneline --graph --decorate --all -20
```

再根据本地提交是否需要保留，选择分支、合并或重新克隆。初学阶段不要直接运行破坏性重置命令。

## 13. 服务器不能访问 GitHub 时的替代方法

### 13.1 临时复制工作文件

如果 Windows 本机能访问 GitHub而服务器不能，可以先在 Windows 本机克隆，再通过 SCP 上传文件。该方式适合临时运行，但服务器上的副本可能不具备方便的远端更新能力。

Windows PowerShell：

```powershell
git clone https://github.com/AYHQChang/2026DeeplearningDemo.git
scp -r .\2026DeeplearningDemo ayit-student:~/courses/
```

上传前应确认服务器目标目录不存在，避免覆盖已有练习。

### 13.2 使用 Git bundle 保留版本历史

如果课程需要完整 Git 历史，可以在联网电脑创建 bundle：

```powershell
git clone https://github.com/AYHQChang/2026DeeplearningDemo.git
cd 2026DeeplearningDemo
git bundle create ..\2026DeeplearningDemo.bundle --all
scp ..\2026DeeplearningDemo.bundle ayit-student:~/courses/
```

在服务器上：

```bash
cd ~/courses
git clone 2026DeeplearningDemo.bundle 2026DeeplearningDemo
```

bundle 适用于离线传递完整提交历史，但服务器若一直无法访问 GitHub，后续更新仍需要重新传输新的 bundle 或由管理员恢复网络访问。

## 14. 不建议直接执行的命令

以下命令可能永久丢失未提交代码或批量删除文件，不应作为一般更新步骤：

```text
git reset --hard
git clean -fd
rm -rf 2026DeeplearningDemo
```

确实需要恢复统一课堂版本时，应先执行 `git status`、备份个人文件，并在明确目标路径和影响范围后进行处理。

## 15. 每次上课前的推荐流程

课程维护者或普通用户可按以下顺序执行：

```bash
ssh ayit-student
cd ~/courses/2026DeeplearningDemo
git status --short
git branch --show-current
git fetch origin
git log --oneline HEAD..origin/main
git pull --ff-only origin main
git status
git log -1 --oneline --decorate
```

如果 `git status --short` 有输出，应先判断这些修改是否需要保留，不直接继续更新。

## 16. 操作记录模板

```text
日期：
操作者：
服务器账号：<你的 Linux 用户名>
服务器主机：172.23.168.7:22
项目目录：

git --version：
当前分支：
更新前最后提交：
git status --short 是否有输出：
执行的 Git 命令：
更新后最后提交：
是否发生冲突：
处理结果：
```

## 17. 本流程的建议边界

- GitHub 是课程代码的发布源，服务器普通账号以 `clone`、`fetch` 和 `pull` 为主。
- 第一次使用 `git clone`；已有仓库后使用 `git pull --ff-only`，不重复克隆。
- 公开仓库的只读操作不需要 GitHub凭据。
- 个人练习通过独立分支或独立目录保存，避免直接污染统一的 `main`。
- 仓库推送应在受控电脑或管理者独立账号中完成，不把写入凭据放在共享账号中。
- 遇到本地修改、分支分叉或证书错误时先保存证据，不使用破坏性命令快速覆盖。

## 18. 参考资料

- [GitHub 仓库：2026DeeplearningDemo](https://github.com/AYHQChang/2026DeeplearningDemo)
- [GitHub Docs：Cloning a repository](https://docs.github.com/en/repositories/creating-and-managing-repositories/cloning-a-repository)
- [GitHub Docs：Caching your GitHub credentials in Git](https://docs.github.com/en/get-started/git-basics/caching-your-github-credentials-in-git)
