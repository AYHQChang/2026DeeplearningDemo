# 深度学习课堂 Demo 实施方案

## 总体方案

在 `D:\Code_for_all\2026DeeplearningDemo` 中建设四个独立但结构一致的实验室：

1. MLP 组件与决策边界实验室
2. CNN 卷积、池化与特征图实验室
3. RNN、LSTM、GRU 记忆能力实验室
4. 小型 Transformer 位置检索与注意力实验室

面向具备 Python 基础的本科二年级课程，每个 Demo 支持 20–30 分钟的“课堂演示＋同步练习”，并提供约 4 小时的综合项目。核心实验完全离线、CPU 可用，GPU 仅作为透明加速；Transformer 只实现从零训练的小型编码器，暂不包含大语言模型、生成模型、预训练权重和自监督学习。

`01_mlp_lab`、`02_cnn_lab`、`03_rnn_lab` 和 `04_transformer_lab` 已完成基础建设。各项目复用相同的教学路径、目录组织、可视化方式和验收标准，不机械复制与具体任务绑定的代码。

## 共享服务器与资源调度策略

课程服务器约有 50 个并发账号和 7 块 GPU。资源策略以“先保证所有账号能运行，再安排少量 GPU 实验”为原则，不依赖额外的调度框架。

### MLP 阶段默认使用 CPU

- MLP 数据和模型很小，GPU 启动与数据传输开销不一定带来速度优势；
- Notebook、`quick_demo()`、`compare()` 和 `demo.py` 的默认设备统一为 `cpu`；
- 每个训练进程通过 `torch.set_num_threads(1)` 限制为 1 个 CPU 线程，避免几十个 Notebook 同时抢占全部 CPU 核；
- 共享课堂不使用 `device="auto"`，防止所有进程自动集中到默认 GPU；
- 同一账号一次只运行一份 Notebook，完成后执行末尾的清理与关机单元格。

### GPU 按账号静态分配

只有明确安排 GPU 实验时，才把 Notebook 中的 `DEVICE = "cpu"` 改为 `DEVICE = "cuda:x"`。`x` 是课堂指定的 GPU 编号，编号从 0 开始；例如使用编号 3 的 GPU 写成 `cuda:3`。

50 个账号可按下表预分配，名单在课前另行维护，不写入公开仓库：

| GPU 编号 | 建议账号数 |
|---:|---:|
| 0 | 8 |
| 1–6 | 每块 7 |

MLP 课堂不要求这 50 个账号同时使用 GPU。CNN、RNN 和 Transformer 实验在正式授课前进行并发压测，控制 Batch size、序列长度和模型规模，并以单账号峰值显存不超过约 2 GB 为初始目标；若压测不满足，再减少同批 GPU 账号数或分时运行。当前不引入 MPS、队列系统或自动 GPU 调度器。

### CPU / GPU 速度教学入口

- 根目录 `device_speed_demo.py` 统一调用四个项目已有的训练 API，不复制模型和训练循环；
- `--lab mlp|cnn|rnn|transformer` 每次只选择一个项目，`--devices cpu,cuda:x` 按顺序运行 CPU 与教师分配的 GPU；
- 禁止 `auto`、裸 `cuda`、多 GPU 同时启动和一次运行全部项目；
- `--target-epochs` 表示完整教学目标，`--preview-epochs` 允许只实跑 1–2 个 Epoch；
- 输出实际设备名称、实跑/目标 Epoch、训练时间、每 Epoch 时间、端到端时间、GPU 峰值已分配显存、线性预估完整时间和预计剩余时间；
- CPU/GPU 加速比只使用训练引擎已同步的训练时间，不把绘图计入；预览 Accuracy 明确标为不可用于最终效果结论；
- GPU 比 CPU 慢也是有效结果：小模型的初始化、传输与 Kernel 启动开销可能大于并行收益。时间只用于教学解释，不作为项目评分。

### Windows 自带电脑策略

- 新增 `5.Windows自带电脑Conda与PyTorch安装.md`，使用 Conda 管理独立环境、使用官方 pip wheel 安装 PyTorch；课程环境固定为 `dl2026` 和 Python 3.11；
- 新增纯 ASCII 的 `check_windows_environment.ps1` 作为安装前只读预检，兼容 Windows PowerShell 5.1；脚本不联网、不安装、不修改驱动或执行策略，只报告系统、Conda、Python、PyTorch 和显卡状态；
- 所有电脑均可选择 CPU；只有 `nvidia-smi` 正常的 NVIDIA 电脑进入 CUDA 候选路线，AMD / Intel 显卡在当前 `cpu|cuda:x` 架构下统一走 CPU；
- `nvidia-smi` 的 CUDA 数值只解释为驱动能力上限，不等同于本机 Toolkit；运行官方 wheel 无需预先安装 `nvcc` 或复制 CUDA DLL；
- Windows 命令采用带日期的 PyTorch 2.11.0 CPU、cu126、cu128 课程快照，根据预检结果手动选择一条；脚本不自动安装或静默选择；
- 不要求重装或升级 Windows；Windows 10 已不再获得 Anaconda 新包验证，因此先尝试原系统 CPU 路线，并保留 Linux 服务器作为备用，正式课堂不现场更换系统或排查试验性 GPU 后端。

### Notebook 结束流程

每份 Notebook 最后保留统一清理单元格：

```python
%reset -f
import gc
import matplotlib.pyplot as plt
import torch
from IPython import get_ipython

plt.close("all")
gc.collect()
if torch.cuda.is_available():
    torch.cuda.empty_cache()

get_ipython().kernel.do_shutdown(restart=False)
```

该单元格先清空当前 Notebook 变量、关闭图片并释放未使用的 CUDA 缓存，再通过 ipykernel 的正常关机请求结束当前 Python 进程和 CUDA 上下文。运行后 VS Code 显示“内核已停止”“请选择内核”或暂时断开连接属于正常现象；再次实验时重新选择内核即可。仅关闭标签页不作为进程已经结束的依据，也不使用 `kill -9`、`os._exit(0)` 等强制结束命令。

## 从 `01_mlp_lab` 固化的教学原则

### 1. 先看到结果，再理解实现

第一次接触项目时，不要求先阅读训练循环。入口 Notebook 应允许直接点击 `Run All`，在几分钟内看到训练结果、关键指标和图片。先建立“输入—训练—输出”的整体印象，再解释其中一部分代码。

推荐学习路径固定为：

```text
00 一键体验
    ↓
01 认识数据与张量
    ↓
02 训练规则与超参数
    ↓
03 训练第一个模型
    ↓
04 组件对比实验
    ↓
05 自主修改练习
    ↓
06 四小时综合项目
```

这一路径从“无需修改代码”逐步过渡到“修改一行参数”和“修改 2–5 行模型代码”，最后用综合项目完成预测、对照、代码修改和证据报告，避免第一次打开项目就面对完整训练框架。

### 2. 每一步都要有即时、可解释的反馈

每份 Notebook 至少提供一项图片输出，并尽量同时给出简短数值结果。图片必须回答当前步骤的问题，而不是仅用于装饰。

| 阶段 | 主要问题 | 推荐图片 |
|---|---|---|
| 一键体验 | 模型最终学到了什么 | 结果总览、最终诊断图 |
| 数据与张量 | 模型看到了什么 | 样本画廊、形状与标签说明图 |
| 训练规则 | 参数如何一步步更新 | Batch 覆盖图、学习率轨迹图 |
| 首次训练 | 模型怎样逐步学会 | 训练阶段图、Loss、Accuracy、梯度图 |
| 组件对比 | 唯一变量带来了什么变化 | 并排对比图、统一尺度的曲线 |
| 自主练习 | 修改前后有什么证据 | 基线图与修改后诊断图 |
| 综合项目 | 能否在资源预算内形成完整证据链 | 闯关计分、实验表、关键机制图 |

Notebook 保存一次经过验证的示例输出。即使现场环境暂时没有运行成功，也可以先观察预期图形；示例结果只说明合理现象，不作为固定答案。

### 3. 一次只改变一个变量

激活函数、优化器、网络宽度、学习率等组件需要分别比较。对照实验保持数据划分、随机种子、训练轮数和其他配置一致，避免同时修改多个参数后无法解释结果来源。

每次练习至少记录：

```text
我的预测 → 唯一修改项 → 数值证据 → 图形证据 → 有边界的结论
```

推荐结论句式：

> 本次只修改了 ______。从 ______ 图可以看到 ______。在当前数据、随机种子和训练轮数下，结果支持 / 不支持 ______。

### 4. 课堂代码短，底层实现只维护一份

Notebook 主要承担叙事、参数设置、调用和观察，不堆放完整的数据处理、训练、日志和绘图代码。命令行脚本与 Notebook 调用同一套核心模块，避免维护两套训练逻辑。

`01_mlp_lab` 已采用以下分层：

```text
01_mlp_lab/
├─ 00_一键体验.ipynb
├─ 01_认识数据与张量.ipynb
├─ 02_训练规则与超参数.ipynb
├─ 03_训练第一个MLP.ipynb
├─ 04_组件对比实验.ipynb
├─ 05_自主修改练习.ipynb
├─ 06_综合项目.ipynb
├─ mlp_lab/
│  ├─ api.py       # Notebook 使用的简洁入口
│  ├─ data.py      # 数据生成、划分和设备选择
│  ├─ model.py     # 首次阅读和修改的模型代码
│  ├─ engine.py    # 训练、验证和对照实验
│  └─ plots.py     # 统一的图片输出
├─ core.py         # 兼容旧脚本的导入入口
├─ demo.py         # VS Code / 命令行入口
├─ test_mlp_smoke.py
└─ README.md
```

Notebook 中的首次体验可以缩短为：

```python
from mlp_lab import quick_demo

result = quick_demo()
```

模型文件应保持短小，优先让课程使用者阅读 `model.py`。训练引擎和绘图模块可以更完整，但不作为第一次进入项目时的阅读材料。`core.py` 只承担兼容导入，不再成为上千行的综合实现文件。

## 统一 Notebook 模板

每个实验室沿用六份基础 Notebook，再增加一份 `06_综合项目.ipynb`，而不是把全部内容塞进一个大文件。

### `00_一键体验.ipynb`

- 不修改代码即可完成一次端到端运行；
- 开头明确说明“先看结果，不要求立即理解全部实现”；
- 输出简短结果表和至少一张核心图片；
- 运行时间优先控制在 1 分钟以内。

### `01_认识数据与张量.ipynb`

- 展示原始样本、标签和训练/测试划分；
- 使用 $\mathcal{D}=\{(\mathbf{x}_i,y_i)\}_{i=1}^{N}$、$X\in\mathbb{R}^{N\times d}$ 等公式连接图片与代码；
- 明确列出关键 tensor shape、dtype 和 device；
- 使用图片说明一个样本如何进入模型；
- 只讲后续训练真正会用到的数据知识。

### `02_训练规则与超参数.ipynb`

- 区分 Sample、Batch、Batch size、Iteration/Step 和 Epoch；
- 用小规模样本图解释每个 Epoch 包含多少次参数更新；
- 用可计算函数展示 Learning rate 太小、合适或太大时的不同轨迹；
- 用一次真实的 PyTorch Step 串联前向计算、Loss、反向传播和参数更新。

### `03_训练第一个模型.ipynb`

- 只训练一个基线模型；
- 展示训练过程，而不只报告最终准确率；
- 引导依次观察模型输出、Loss、训练/测试差距和梯度；
- 用少量代码把训练现象与模型结构连接起来。

### `04_组件对比实验.ipynb`

- 一次只比较一个组件；
- 并排绘图并统一坐标、颜色和指标口径；
- 先提出预测，再运行实验；
- 不采用“只看最高 Accuracy”的排名式结论。

### `05_自主修改练习.ipynb`

- 参数修改区集中放在一个明显的单元格；
- 首轮只需修改一行参数；
- 进阶任务再进入短小的 `model.py`，修改 2–5 行关键代码；
- 输出基线与新结果，保留实验记录表和结论模板。

### `06_综合项目.ipynb`

- 总工作量按约 240 分钟设计，但正式训练次数和单次计算量必须受限；
- 至少包含问题预测、三组单变量对照、一处最小代码修改和 300–500 字证据报告；
- 使用准确率、鲁棒性、参数量或更新次数等多维约束，不以运行时间或单一最高 Accuracy 排名；
- 默认 CPU 单线程，明确正式训练次数上限，并用配置校验阻止误填大模型或超长训练；
- 不预填参考答案；允许未达到挑战目标，但必须解释证据、瓶颈和结论边界。

## 20–30 分钟课堂组织建议

| 时间 | 课堂活动 | 展示重点 |
|---:|---|---|
| 0–3 分钟 | 展示已保存的结果图并提出预测问题 | 先建立目标，不先解释全部代码 |
| 3–7 分钟 | 运行 `00_一键体验` | 一次完整的数据到结果流程 |
| 7–12 分钟 | 打开数据与张量 Notebook | 数学数据集、Tensor、shape 和划分 |
| 12–17 分钟 | 打开训练规则 Notebook | Batch、Epoch、Loss、梯度和学习率 |
| 17–22 分钟 | 观察首次训练或组件对比 | 决策过程、曲线和单变量原则 |
| 22–28 分钟 | 修改一个参数并重新运行 | 修改前后的数值与图片证据 |
| 28–30 分钟 | 填写结论并说明适用边界 | 不把单次结果概括为普遍规律 |

讲解时先问“你预计图会怎样变化”，再运行单元格；结果出现后先读图，再回到少量关键代码。代码解释集中在输入 shape、模型结构、损失、优化和输出含义，不逐行朗读 Python 语法。

## 实验内容

- **MLP 实验室**
  - 使用月牙、同心圆、螺旋等现场生成的二维数据。
  - 公平比较网络深度/宽度、激活函数、合理的损失函数组合、优化器、学习率和 Dropout。
  - 展示决策边界随 Epoch 的形成过程、loss、accuracy、误判点和 gradient norm。
  - 练习挑战：在固定参数预算下完成螺旋分类。

- **CNN 实验室**
  - 第一版使用 scikit-learn 内置的 `load_digits`：1797 张 `8×8` 灰度数字图片，完全离线，不增加 `torchvision` 或下载步骤。
  - 教学主线固定为“图片 → 像素矩阵 → 四维 Tensor → 局部卷积 → 多通道特征图 → 池化 → 分类”，不从 API 参数表开始。
  - 六份基础 Notebook 为：`00_一键体验`、`01_图片数据与四维张量`、`02_卷积核与池化`、`03_训练第一个CNN`、`04_CNN组件对比实验`、`05_自主修改练习`；`06_CNN识别侦探项目`承担四小时综合任务。
  - `00` 展示预测概率、混淆矩阵和错误样本；`01` 对齐图片、像素数值及 `[B,C,H,W]`；`02` 用人工卷积核和池化图片解释机制；`03` 训练两层小型 CNN 并展示不同层的 Feature maps。
  - `04` 第一版只比较 Max Pooling、Average Pooling 和不使用 Pooling，同时报告特征图尺寸、参数量、训练时间、干净测试集与平移测试集表现。
  - `05` 把通道数、kernel size、pooling 和 dropout 集中在一个参数单元格，每次只修改一项。
  - 课堂建议拆成两个 25–30 分钟单元：第一单元完成 `00–02`，解释 CNN 为什么适合图片；第二单元完成 `03–05`，解释 CNN 怎样学习并完成单变量实验。
  - 不把 Feature map 解释成模型思维的完整证明，不预设 CNN 必然优于 MLP，也不把最高 Accuracy 作为唯一结论。

### CNN 从 `8×8` 扩展到 `28×28` 的约束

第一版用 `8×8` 降低认知与计算负担，但数据和模型接口不得把空间尺寸写死在训练循环中：

- 数据统一输出 `[N,C,H,W]`，数据对象记录 `image_size`、通道数、类别名和像素范围；
- CNN 构造函数显式接收 `image_size`，分类层输入维度由卷积与池化后的空间尺寸计算；
- 绘图函数从 Tensor shape 获取高宽，像素矩阵标注仅在小图上开启；
- 冒烟测试除 `8×8` 真实数据外，额外验证随机 `[B,1,28,28]` 可以前向得到 `[B,10]` logits；
- 后续加入 MNIST 或其他 `28×28` 手写字符时，只新增离线数据适配与类别说明，不复制训练、模型、设备或绘图框架；
- `28×28` 数据包应在课前随仓库或课程材料提供并校验，不在正式课堂临时联网下载。

暂不为尚未选定的 `28×28` 数据格式设计插件系统或通用数据注册器；真实数据确定后，再实现一个返回同一数据对象的加载函数。

- **RNN 实验室**
  - 核心任务采用“延迟回忆三分类”：首个时间步写入红、绿或蓝的秘密符号，中间只出现与标签无关的随机噪声，末尾出现查询标记，模型根据最后的隐藏表示回答开头的符号。
  - 数据全部离线生成，输入统一为 `[Batch, Time, Feature]`。6 个特征通道分别表示 3 类秘密、2 路随机噪声和 1 个查询标记；训练、验证、测试集分别生成并保持类别近似平衡。
  - `00` 用短序列 LSTM 一键完成训练、预测概率和混淆矩阵；`01` 对齐 Batch、Time、Feature 与 tensor 索引；`02` 展开循环结构并观察隐藏状态和输入梯度；`03` 训练 Vanilla RNN；`04` 在相同条件下比较 RNN、LSTM、GRU；`05` 用“基线—单项修改—证据—有限结论”完成自主练习。
  - 核心长度为 12、30、60。课堂快速模式只运行 12 和 30；长度 60 的完整对比由课堂统一分批启动。
  - 比较同时报告测试 Accuracy、参数量和训练时间。相同 hidden size 不等于相同参数量，因此不将其表述为容量完全公平的比较，也不预设 LSTM 或 GRU 必然获胜。
  - 默认 `device="cpu"`、`torch.set_num_threads(1)`、`DataLoader(num_workers=0)`。约 50 个账号面对 48 个逻辑 CPU 时，普通单模型训练可以同步进行；六组或九组对比按区域分批启动，避免瞬时满载。
  - 7 块 GPU 只作为明确指定 `cuda:x` 的可选加速资源，不使用 `auto`。该任务的数据和模型很小，正式课堂不要求占用 GPU。
  - 课前用 1、10、25、50 个 CPU 进程进行并发测试，记录单任务与并发运行时间、系统负载和内存；单任务目标小于 30 秒，50 并发无 OOM，完整对比若明显变慢则继续分批。
  - 每份 Notebook 都输出与当前问题直接相关的图片，并在最后提供正常关闭内核的清理单元格。
  - 后续 `11_正弦波序列预测.ipynb` 再引入滑动窗口和连续值预测；第一版暂不加入文本生成、可变长度与 packing、双向结构、注意力机制或自定义 LSTM/GRU 门实现。

## `04_transformer_lab` 小型 Transformer 任务与实施结果

### 1. 核心任务与学习目标

项目采用“注意力寻宝：找回序列首位颜色”的三分类任务。每条样本包含一段颜色 token，末尾追加一个 `QUERY` token；标签是第一位置的颜色，模型读取查询位置的表示并预测红、绿或蓝。

数据生成必须让每条序列中三种颜色的总数量相同，只改变它们的位置顺序。这样，无位置编码的模型不能通过颜色计数猜答案，Positional Encoding 的作用才有明确的对照依据。

该任务与 RNN 延迟回忆形成自然衔接，但教学重点不同：

- RNN 按时间顺序压缩历史，Transformer 让查询位置直接关注全部 token；
- token id 输入为 `[B,T]`，Embedding 后为 `[B,T,d_model]`；
- 没有位置信息时，模型无法可靠区分第一位置与其他位置；
- Attention map 展示查询位置向哪些 token 分配权重，但不把注意力权重表述为完整的因果解释或模型思维；
- 数据完全离线生成，只表达位置检索能力，不引入自然语言语义。

第一版固定长度，不加入 Padding、Tokenizer 和词表文件。等固定长度任务稳定后，再决定是否加入变长序列与 Padding Mask；不为尚未使用的输入形式提前建设通用数据管线。

### 2. Notebook 路线

| 顺序 | Notebook | 核心任务 | 主要输出 |
|---:|---|---|---|
| 0 | `00_一键体验.ipynb` | 直接运行一次位置检索 | 序列条带、预测概率、训练曲线、查询注意力 |
| 1 | `01_Token与三维张量.ipynb` | 对齐 token id、Embedding 与 `[B,T,D]` | 样本分解图、shape 流程、Embedding 热力图 |
| 2 | `02_位置编码与QKV.ipynb` | 理解位置、点积、缩放和 Softmax | 位置编码图、单头注意力计算、Q/K/V shape |
| 3 | `03_训练第一个Transformer.ipynb` | 训练单层 Encoder 分类器 | Loss、Accuracy、混淆矩阵、多头 Attention map |
| 4 | `04_Transformer组件对比.ipynb` | 单变量比较位置编码和注意力头数 | 统一尺度曲线、准确率、参数量、长序列测试 |
| 5 | `05_自主修改练习.ipynb` | 修改一个配置或 2–5 行模型代码 | 基线与修改后诊断、实验记录 |
| 6 | `06_注意力寻宝项目.ipynb` | 完成四小时综合挑战 | 徽章计分、三组对照、代码修改、证据报告 |

`02` 只用一个小矩阵手算并可视化 Attention，不训练模型；`03` 才进入完整训练。组件比较先以“有/无位置编码”为主实验，再在 `d_model` 可整除的前提下比较 1、2、4 个 Head。每次只改变一个变量，不预设 Head 越多或层数越深越好。

### 3. 数据、模型与代码架构

    04_transformer_lab/
    ├─ 00_一键体验.ipynb
    ├─ 01_Token与三维张量.ipynb
    ├─ 02_位置编码与QKV.ipynb
    ├─ 03_训练第一个Transformer.ipynb
    ├─ 04_Transformer组件对比.ipynb
    ├─ 05_自主修改练习.ipynb
    ├─ 06_注意力寻宝项目.ipynb
    ├─ transformer_lab/
    │  ├─ api.py       # quick_demo()、compare()、challenge_report()
    │  ├─ data.py      # 离线序列生成、独立划分、设备与随机种子
    │  ├─ model.py     # Embedding、位置编码、小型 Encoder 和分类头
    │  ├─ engine.py    # 配置校验、训练、评价和单变量对照
    │  └─ plots.py     # 序列、QKV、Attention、曲线和混淆矩阵
    ├─ demo.py
    ├─ test_transformer_smoke.py
    └─ README.md

数据契约：

- 词表只含 3 个颜色 token 和 1 个 `QUERY` token；
- 默认内容长度为 12，三种颜色各出现 4 次，再追加查询 token，因此输入总长度为 13；
- 训练、验证、测试集使用不同随机种子分别生成并保持标签平衡，返回前检查精确 token 序列不跨集合重复；
- 扩展内容长度为 24，仍保持三种颜色计数相同，用于长度迁移测试；
- 返回 token ids、标签、类别名和词表说明；第一版没有 Padding，因此不传伪造的 mask。

模型契约：

- 使用 `nn.Embedding` 和正弦 Positional Encoding；
- Encoder Block 只薄封装 PyTorch `nn.MultiheadAttention(batch_first=True)`、Residual、LayerNorm 和两层 Feed-forward Network；
- 不手写多头拆分和矩阵乘法实现，只额外返回最后一层注意力权重用于教学绘图；
- 默认 `d_model=32`、`num_heads=4`、`num_layers=1`、`dim_feedforward=64`、`dropout=0.1`；
- 使用末尾 `QUERY` 表示连接三分类头，输出 `[B,3]` logits；
- `model.py` 保持短小；进阶修改点优先放在 Feed-forward 激活映射或位置编码开关，不复制整个 Encoder。

公开接口优先为 `make_retrieval_data()`、`TinyTransformerClassifier`、`run_experiment()`、`quick_demo()`、`compare()` 和 `challenge_report()`。

### 4. 共享服务器安全预算

默认配置暂定：

| 项目 | 默认值 |
|---|---:|
| 设备 | CPU |
| CPU 线程 | 1 |
| DataLoader workers | 0 |
| 内容长度 / 输入总长度 | 12 / 13 |
| 训练 / 验证 / 测试样本 | 1200 / 300 / 300 |
| Batch size | 64 |
| Epoch | 12 |
| `d_model` / Head / Layer | 32 / 4 / 1 |
| Feed-forward 宽度 | 64 |

训练入口必须在分配模型前执行配置校验，初始硬上限为：

- 禁止共享课堂使用 `device="auto"`；默认 CPU，只有明确分配后才允许 `cuda:x`；
- `content_length≤24`、`train_samples≤3000`、`epochs≤30`、`batch_size≤128`；
- `d_model≤64`、`num_heads≤4`、`num_layers≤2`、`dim_feedforward≤128`；
- 强制 `content_length` 能被 3 整除，且 `d_model % num_heads == 0`；
- 单次参数更新数≤1500、模型参数量≤150000；
- 增加组合预算：`train_samples × epochs × num_layers × input_length²≤12000000`，避免各参数单独合法但组合后仍过重；
- 只保存少量样本和最后一层的注意力图，不缓存全部 Batch、全部 Epoch 的 `[B,H,T,T]` 权重。

以上阈值是实现前的保守起点，不用扩大上限来制造四小时工作量。验收时分别用 1、10、25、50 个 CPU 进程压测；目标为单次基线训练小于 30 秒、`00` 完整运行小于 1 分钟、50 并发无 OOM。若不满足，先减少样本、Epoch 或同时启动的对比组数，不切换到自动抢占 GPU。

### 5. 四小时综合项目

| 阶段 | 建议时间 | 阶段成果 |
|---|---:|---|
| 基线与任务预测 | 20 分钟 | 基线结果、检索位置预测 |
| Token、Embedding 与位置分析 | 35 分钟 | shape 表和位置编码图 |
| Q/K/V 与 Attention 取证 | 40 分钟 | 一次手算、查询注意力解释 |
| 三组单变量对照 | 55 分钟 | 位置编码、Head 或长度实验表 |
| 阅读并修改 `model.py` | 45 分钟 | 一处最小修改、烟雾测试 |
| 预算内挑战 | 25 分钟 | 最多 3 次尝试、最佳配置 |
| 证据报告 | 20 分钟 | 300–500 字有限结论 |
| **合计** | **240 分钟** | **约 4 小时** |

正式训练最多 8 次。挑战计分同时考虑测试 Accuracy、内容长度 24 的迁移表现、参数量和注意力组合预算；不使用运行时间计分。未集齐徽章不等于失败，只要记录预测、唯一修改项、数值证据、图形证据和下一步。

### 6. 验收与明确边界

- 冒烟测试覆盖颜色计数相同、三个数据集无精确 token 序列重叠、shape、位置编码、Attention 每行和约为 1、`[B,3]` 输出、CPU 小训练和全部安全上限；
- 七份 Notebook 使用 `nbclient.NotebookClient` 执行到关机单元格之前，要求 0 个代码单元错误；
- `00–05` 保存一次经过验证的示例输出，`06` 保留填写区，不预填实验结论；
- 记录基线、无位置编码、不同 Head 和长度迁移的实测结果，不承诺 Transformer 必然优于 RNN；
- 断网运行，不新增 `transformers`、`torchtext`、Tokenizer、预训练模型或在线数据；
- 第一版不实现 Decoder、Causal Mask、文本生成、机器翻译、LLM 微调、变长 Padding 和大词表；这些内容只有在基础任务验收后才另立项目。

## 1.1 衔接实验：MLP 房价回归

以 `01_mlp_lab/11_房价预测.ipynb` 衔接分类与回归；一份连续 Notebook 完成全流程，作为现有六步 MLP 实验后的扩展。建议先完成 Tensor、训练规则和首次 MLP 训练。

- 数据默认采用 600 套离线生成的模拟房源，输入为面积、房间数、房龄、距中心距离，目标为万元单位总价；每次展示均说明数据为模拟，不作为真实市场估价。
- 教学顺序：房源表 → 特征散点与分布 → 60/20/20 划分 → 训练统计量标准化 → `[B,4]` 到 `[B,1]` → 五步参数更新 → 分阶段预测 → 组件比较 → 最终测试 → 自定义房源与面积响应曲线。
- 使用训练集拟合输入和目标标准化器。验证集选择超参数与最佳轮次；所有调参完成后才查看测试预测。比较时统一万元 MAE，不能直接比较不同训练损失的数值。
- 明确区分 MSE、MAE、Huber（标准化尺度 delta=1）、Adam、无动量 SGD 和 RMSprop。参数区集中展示隐藏层、激活、学习率、Batch、Epoch、权重衰减及随机种子。
- 课堂前半段用 20–30 分钟完成数据到首次预测；后半段每次只修改一项，先提出预期、运行后读图，再写出验证 MAE 与曲线支持的有限结论。
- 最后提供训练均价基线，解释 MAE、RMSE 的金额单位及 R² 不等于准确率。新房源沿用训练标准化器，外推给予提示，不把面积响应曲线当作因果效应。
- Notebook 保留可读的短配置和调用，模型、训练在 `house_price.py`，绘图在 `house_price_plots.py`。CSV 替换示例使用相同四特征格式，不添加数据下载或训练框架。
- CPU 单线程、DataLoader 无子进程为默认。GPU 使用明确的 `cuda:x` 编号注释；训练结果移回 CPU，Notebook 最后一格清理并关闭当前内核。
- 验证包括数据划分互斥、标准化不泄漏、九种损失/优化器组合、原单位预测、最佳轮次还原、CPU 可复现、缺失值与无效配置提示，以及完整 Notebook 图形输出。

## 代码与使用接口

- 每个新建或完成综合优化的实验室包含六份基础中文 Notebook、一份四小时综合项目、VS Code 一键运行脚本、共享模块、简短 README 和一个冒烟测试。
- 对外优先提供 `quick_demo()`、`compare()` 一类简洁入口，Notebook 中一次调用控制在少量代码。
- 模型、训练与绘图逻辑只维护一份，Notebook 和命令行入口不得复制训练循环。
- 课堂 Notebook 和公开命令统一使用 `--device cpu|cuda:编号`，默认 `cpu`；共享训练入口拒绝 `auto`，避免集中占用默认 GPU。
- 脚本统一支持 `--mode fast|extended`，课堂默认 `fast`。
- 脚本保留 `--seed` 和实验组件选择参数。
- 显式选择不可用的 CUDA 时给出中文错误提示；损失函数与输出层的不合法组合不静默执行。
- 注释以中文解释教学意图、tensor shape、数学约束和预期现象，保留必要英文关键词；不逐行翻译显而易见的 Python 语句。
- 依赖限制为 PyTorch、NumPy、Matplotlib、scikit-learn 和 Jupyter，不引入训练框架、在线服务或额外配置系统。

## 验收与测试

- 六份基础 Notebook 和一份综合项目均能执行 `Restart Kernel + Run All`；验证时执行到最终关机单元格之前，且各自至少产生一项与学习目标直接相关的图片。
- Notebook 保存一次成功运行的示例输出，但不把示例指标写成唯一正确答案。
- 已实现实验室在普通 Windows 或 Linux CPU 上均可完整运行；Transformer 实施后也必须满足单次训练小于 30 秒、课堂完整对比控制在 3–5 分钟的目标。
- 在课程环境中验证默认 CPU、指定 GPU 编号和编号越界提示；CPU/GPU 比较预期趋势和指标区间，不要求逐位相同。
- Windows 自带电脑先运行 PowerShell 只读预检，再分别验收无 Conda、CPU build、可用 NVIDIA CUDA build 等分支；缺项只报告，不触发自动安装。
- 固定数据划分、随机种子和对照条件；每次组件比较只改变一个目标变量。
- Notebook、命令行脚本和冒烟测试复用同一模型与训练逻辑。
- 所有脚本从项目根目录和实验室自身目录运行测试。
- 断网测试确认没有运行时下载、远程 API、摄像头或云模型依赖。
- 公开仓库中的服务器 IP、SSH 端口、登录用户名、密码、主机名和指纹使用占位符。
- 为每个实验提供预期现象和合理指标范围，避免将偶然的一次训练结果写成固定答案。

## 已确定的默认条件与当前进度

- `01_mlp_lab` 已完成六份基础 Notebook 和四小时闯关项目，可作为后续实验室的参考实现。
- 先完整演示，再通过修改配置和补全约 2–5 行关键代码进行同步练习。
- 环境可在课前安装，正式课堂完全离线；不制作便携 Python 环境或离线 wheel 大包。
- CPU 是共享课堂默认值，GPU 只按分配编号使用；两者复用同一套代码。
- `02_cnn_lab` 已完成六份基础 Notebook 和四小时识别侦探项目；`03_rnn_lab` 已完成六份基础延迟回忆 Notebook，并保留独立冒烟测试。
- `04_transformer_lab` 已完成模块化代码、七份 Notebook、子项目 README、命令行入口和独立冒烟测试；`00–05` 保存验证输出，`06` 保持填写区为空。
- 当前课程环境中，默认 CPU 基线训练约 1 秒、测试 Accuracy 约 99.7%、长度 24 迁移约 92.7%，`00` 完整执行约 7.7 秒；这些数值只用于单机校准，不承诺在其他机器复现。
- 实际教学服务器的 10、25、50 并发压测仍须在课前分级完成；未通过时先缩小样本、Epoch 或同步对比组数，不放宽硬上限，也不改为自动占用 GPU。
- 根目录已增加四项目统一 CPU/GPU 速度入口及独立冒烟测试；本次开发机上的 2-Epoch 预览显示四个小模型均为 CPU 更快，进一步说明正式授课不能预设 GPU 必然加速。
- Windows 自带电脑指南与只读预检脚本已加入根目录；安装后继续复用 `check_environment.py` 和各项目冒烟测试，不另建第二套 Python 验收逻辑。
- 新实验室先复用目录和教学节奏，再按任务需要实现最少的公共模块，不提前建设通用训练框架。
