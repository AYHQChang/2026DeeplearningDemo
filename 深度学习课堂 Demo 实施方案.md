# 深度学习课堂 Demo 实施方案

## 总体方案

在 `D:\Code_for_all\2026DeeplearningDemo` 中建设三个独立但结构一致的实验室：

1. MLP 组件与决策边界实验室
2. CNN 卷积、池化与特征图实验室
3. RNN、LSTM、GRU 记忆能力实验室

面向只掌握 Python 基础的大二学生，每个 Demo 支持 20–30 分钟的“教师演示＋学生跟练”。核心实验完全离线、CPU 可用，GPU 仅作为透明加速；暂不包含 Transformer、生成模型和自监督学习。

## 实验内容

- **MLP 实验室**
  - 使用月牙、同心圆、螺旋等现场生成的二维数据。
  - 公平比较网络深度/宽度、激活函数、合理的损失函数组合、优化器、学习率和 Dropout。
  - 展示决策边界、loss、accuracy 和 gradient norm。
  - 学生挑战：在固定参数预算下完成螺旋分类。

- **CNN 实验室**
  - 使用完全离线的小型手写数字数据。
  - 先直接演示边缘检测、模糊和锐化卷积核，再训练小型 CNN。
  - 比较 Max/Average/无池化，以及 kernel、stride、padding、BatchNorm、Dropout。
  - 展示 tensor shape、feature maps、参数量、混淆矩阵、错误样本和抗噪性能。
  - 学生挑战：在模型大小、准确率和抗噪性之间选择最佳配置。

- **RNN 实验室**
  - 使用“记住序列开头秘密符号”的受控长期依赖任务。
  - 比较 Vanilla RNN、LSTM、GRU，以及序列长度、hidden size、层数、双向结构和 gradient clipping。
  - 展示准确率随序列长度变化、梯度范数、参数量和训练时间。
  - 趣味彩蛋为小型中文字符续写器，不用其随机生成质量判断三种循环单元优劣。
  - 学生挑战：用尽量少的参数通过长序列记忆测试。

## 代码与使用接口

- 每个实验室包含中文叙事 Notebook、VS Code 一键运行脚本、共享核心模块和简短 README；模型与训练逻辑只维护一份。
- Notebook 采用“学习目标→预测→基线→单变量对照→可视化→填空练习→小结”结构，并保证 `Restart Kernel + Run All` 成功。
- 脚本统一支持：
  - `--device auto|cpu|cuda`，默认 `auto`，无 CUDA 时自动使用 CPU。
  - `--mode fast|extended`，课堂默认 `fast`。
  - `--seed` 和实验组件选择参数。
- 显式选择不可用的 CUDA 时给出中文错误提示；损失函数与输出层的不合法组合不静默执行。
- 注释以中文解释教学意图、tensor shape、数学约束和预期现象，保留必要英文关键词；不逐行翻译显而易见的 Python 语句。
- 依赖限制为 PyTorch、NumPy、Matplotlib、scikit-learn 和 Jupyter，不引入训练框架、在线服务或额外配置系统。

## 验收与测试

- 三个实验室在普通 Windows CPU 上均可完整运行；单次训练目标为 30–60 秒，课堂完整对比控制在 3–5 分钟。
- 当前开发机安装 PyTorch 后，分别验证强制 CPU、自动设备和 CUDA 路径；CPU/GPU 比较预期趋势和指标区间，不要求逐位相同。
- 固定数据划分、随机种子和对照条件；每次组件比较只改变一个目标变量。
- 所有 Notebook 执行“重启内核并运行全部单元格”测试，所有脚本从项目根目录和自身目录运行测试。
- 断网测试确认没有运行时下载、远程 API、摄像头或云模型依赖。
- 为每个实验提供预期现象和合理指标范围，避免将偶然的一次训练结果写成固定答案。

## 已确定的默认条件

- 教师完整演示，学生通过修改配置和补全约 2–5 行关键代码跟练。
- 环境可在课前安装，正式课堂完全离线；不制作便携 Python 环境或离线 wheel 大包。
- CPU 是最低兼容基线，GPU 不是必需条件，也不维护两套分叉代码。
- 当前 Demo 目录为空且不是 Git 仓库；当前 Codex Python 环境缺少 PyTorch、Matplotlib、scikit-learn 和 Jupyter，因此正式实施首先建立并记录经过验证的课程环境。