# Windows 笔记本安装与配置 Git

## 1. 先区分 Windows Git 和服务器 Git

课程 Linux 服务器已经安装 Git。连接服务器后，在远程终端运行的是服务器 Git。

个人笔记本安装的 Git for Windows 用于：

- 在 Windows 本地克隆项目；
- 在本地查看代码版本和修改；
- 让本地 VS Code 使用源代码管理功能；
- 为以后学习提交、分支和协作做准备。

两套 Git 互不替代：

```text
Windows PowerShell 中的 git → 笔记本本地 Git
Remote-SSH 终端中的 git   → Linux 服务器 Git
```

这份教程只安装个人笔记本的 Windows Git。实验室电脑带有还原功能，不重复安装 Windows Git。

---

## 2. 安装前先检查

打开新的 Windows PowerShell，运行：

```powershell
git --version
where.exe git
```

如果能看到 Git 版本，并且 `where.exe git` 显示有效路径，说明已经安装。关闭并重新打开 VS Code 后继续使用，不需要重复安装。

如果系统提示无法识别 `git`，再按照后续步骤安装。

---

## 3. 使用班长发放的安装包

本课程使用：

```text
Git-2.55.0.5-64-bit.exe
```

不要在上课时通过 VS Code 的弹窗跳转网页重新下载。使用班长统一发放、课堂核验过的文件。

### 可选：核对安装包

在安装包所在文件夹的地址栏输入 `powershell`，按 Enter，然后运行：

```powershell
Get-AuthenticodeSignature ".\Git-2.55.0.5-64-bit.exe" |
    Format-List Status, StatusMessage

Get-FileHash -Algorithm SHA256 ".\Git-2.55.0.5-64-bit.exe"
```

课堂文件的签名状态应为：

```text
Status : Valid
```

SHA-256 应为：

```text
D065A4E23C3D9A6B5073D609B5BE0830227EC3CA053C083BA385061DDFAF94C6
```

如果签名无效或哈希不同，先停止安装并联系教师确认文件来源。

---

## 4. 按安装向导操作

双击 `Git-2.55.0.5-64-bit.exe`。

如果 Windows 显示用户账户控制提示，先确认文件名和发布者与班级文件一致，再允许安装。不要对未知安装包关闭安全检查。

### 第 1 页：Information / License

阅读信息，点击 `Next`。

### 第 2 页：Select Destination Location

个人笔记本使用默认目录即可：

```text
C:\Program Files\Git
```

Git for Windows 能正确处理这个官方默认路径。除非 C 盘空间不足，不建议临时修改。

### 第 3 页：Select Components

保留默认组件即可。常见默认项包括：

- Windows Explorer integration；
- Git Bash Here；
- Git GUI Here；
- Git LFS；
- 文件关联。

桌面图标可以按个人习惯选择，不影响课程。

### 第 4 页：Start Menu Folder

保留默认的 `Git`，点击 `Next`。

### 第 5 页：Choosing the default editor used by Git

为了避免初学者误进入 Vim，可以选择：

```text
Use Notepad as Git's default editor
```

如果列表中没有该项，保留默认也不影响本课程，因为课堂提交命令会使用 `git commit -m "说明"`。

不要选择笔记本中无法启动的编辑器路径。

### 第 6 页：Adjusting the name of the initial branch

选择覆盖默认分支名称，并填写：

```text
main
```

如果安装器界面与此不同，也可以保留默认，安装后再运行：

```powershell
git config --global init.defaultBranch main
```

### 第 7 页：Adjusting your PATH environment

这是最重要的一页。选择中间选项：

```text
Git from the command line and also from 3rd-party software
```

这个选项会让安装器把安全的 Git 命令入口加入 Windows `PATH`，从而让 PowerShell、VS Code 和其他软件找到 Git。

不要选择只允许 Git Bash 使用 Git；否则 PowerShell 和 VS Code 可能仍然找不到 `git`。

### 第 8 页：Choosing the SSH executable

选择：

```text
Use bundled OpenSSH
```

它主要供 Git 自己建立 SSH 连接使用。VS Code Remote-SSH 仍可使用 Windows OpenSSH，两者不要手工混合路径。

### 第 9 页：Choosing HTTPS transport backend

保留默认：

```text
Use the OpenSSL library
```

### 第 10 页：Configuring line ending conversions

本课程代码还会在 Linux 服务器上使用，建议选择：

```text
Checkout as-is, commit Unix-style line endings
```

这样可以减少 Windows 与 Linux 之间仅由换行符造成的大量修改。

### 第 11 页：Terminal emulator

保留默认的 MinTTY 即可。它影响 Git Bash，不影响 VS Code 中的 PowerShell。

### 第 12 页：`git pull` behavior

保留安装器默认选项。课程更新时会明确使用：

```powershell
git pull --ff-only origin main
```

### 第 13 页：Credential helper

保留 Git Credential Manager。以后使用自己的 GitHub 账号时，它可以帮助管理身份验证。

不要在公共电脑中保存个人 GitHub 密码或 Token。

### 第 14 页：Extra options

保留文件系统缓存等默认选项。初学阶段不要启用实验性功能或符号链接选项。

点击 `Install`，等待完成，再点击 `Finish`。

---

## 5. 环境变量是怎样设置的

### 什么是 PATH

`PATH` 是 Windows 查找命令的目录列表。输入：

```powershell
git --version
```

Windows 会按 `PATH` 中的顺序寻找 `git.exe`。

安装器中的“Git from the command line and also from 3rd-party software”通常会自动加入：

```text
C:\Program Files\Git\cmd
```

因此正常情况下不需要手工编辑环境变量。

### 安装后必须重开程序

安装完成后：

1. 关闭所有 PowerShell；
2. 关闭所有 VS Code；
3. 重新打开 PowerShell；
4. 重新打开课程专用 VS Code。

已经打开的程序不会自动读取刚刚更新的 `PATH`。

### 检查 PATH 中的 Git

在新 PowerShell 中运行：

```powershell
where.exe git
($env:Path -split ';') | Where-Object { $_ -match '\\Git\\cmd$' }
```

正常情况下会看到：

```text
C:\Program Files\Git\cmd\git.exe
C:\Program Files\Git\cmd
```

---

## 6. PowerShell 仍然找不到 Git 时

先重新运行安装程序，确认 PATH 页面选择的是：

```text
Git from the command line and also from 3rd-party software
```

如果仍然不行，再手工添加用户环境变量。

### 手工添加步骤

1. 在 Windows 搜索框输入“环境变量”；
2. 打开“编辑账户的环境变量”；
3. 在“用户变量”中选择 `Path`；
4. 点击“编辑”；
5. 点击“新建”；
6. 添加：

```text
C:\Program Files\Git\cmd
```

7. 连续点击“确定”保存；
8. 关闭并重新打开 PowerShell 和 VS Code。

如果 Git 安装到了其他位置，就添加实际安装目录下的 `cmd`，例如：

```text
D:\Git\cmd
```

### 不要添加这些目录

不要把下面目录手工加入 Windows 全局或用户 `PATH`：

```text
C:\Program Files\Git\usr\bin
C:\Program Files\Git\mingw64\bin
```

这些目录包含 Git 自己使用的 Unix 工具和动态库，整体加入系统 PATH 可能与其他 Windows 软件发生冲突。只添加 `Git\cmd`。

---

## 7. 验证 Git 是否安装成功

在新打开的 PowerShell 中依次运行：

```powershell
git --version
where.exe git
git --exec-path
```

预期结果类似：

```text
git version 2.55.0.windows.5
C:\Program Files\Git\cmd\git.exe
C:/Program Files/Git/mingw64/libexec/git-core
```

不同安装位置会显示不同路径，这是正常的。

再打开课程专用 VS Code 1.98.2，新建终端并运行：

```powershell
git --version
where.exe git
```

如果 PowerShell 和 VS Code 终端都能看到 Git 版本及路径，说明 Git 软件和环境变量均安装成功。

安装成功不要求此时能够访问 GitHub。网络连接失败与 Git 软件安装失败是两类问题。

---

## 8. 配置提交身份

只执行 `clone` 和 `pull` 时，可以暂不配置姓名与邮箱。准备在本地创建提交前，再执行：

```powershell
git config --global user.name "你的姓名"
git config --global user.email "你的邮箱"
git config --global init.defaultBranch main
```

不要原样复制示例文字，要换成自己的信息。公开仓库不想显示私人邮箱时，可以按照 GitHub 账号设置使用 noreply 邮箱。

检查配置：

```powershell
git config --global --list --show-origin
```

这些是 Git 配置，不是 Windows 环境变量。

---

## 9. 什么才算安装成功

下面项目全部满足，才算完成：

```text
□ 安装包数字签名有效
□ git --version 显示版本号
□ where.exe git 能找到 Git\cmd\git.exe
□ 新 PowerShell 能执行 Git
□ 重新打开的 VS Code 终端也能执行 Git
□ Windows PATH 中只有必要的 Git\cmd 入口
□ 没有把 usr\bin 或 mingw64\bin 加入全局 PATH
```

---

## 10. 常见问题

### VS Code 仍然显示“Download Git”

完全退出所有 VS Code 窗口，再重新打开课程专用 VS Code。如果终端中的 `git --version` 正常，等待 VS Code 重新检测。

### PowerShell 可以使用 Git，VS Code 不行

通常是 VS Code 在安装 Git 之前就已打开。完全退出后重新启动，不要重复安装 Git。

### `where.exe git` 显示多条路径

说明电脑可能安装了多套 Git。先记录完整输出，不要立即删除任何软件。课程使用的第一条应当是本教程安装的 Git 路径。

### 安装器再次显示修改、修复或卸载

说明 Git 已经安装。先取消安装器，回到 PowerShell 执行 `git --version` 和 `where.exe git`，不要重复覆盖。

### GitHub 无法访问

先确认 `git --version` 是否正常。如果版本和路径正常，Git 安装已经成功；GitHub 超时、代理或校园网络属于网络问题。

### 在服务器终端看到不同的 Git 版本

正常。Windows Git 和服务器 Git 是两套独立软件。课程服务器上的版本由服务器管理员维护。

---

## 11. 官方参考

- [Git for Windows 官方安装入口](https://git-scm.com/install/windows)
- [Pro Git：Installing Git](https://git-scm.com/book/en/v2/Getting-Started-Installing-Git)
- [Git for Windows：Git wrapper 与 PATH](https://gitforwindows.org/git-wrapper.html)
