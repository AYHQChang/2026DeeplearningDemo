# 认识课程项目与工程化

## 1. 为什么先认识项目结构

以前写 Python 练习时，我们可能只需要一个文件：

```text
练习.py
```

但是一个完整项目通常还需要保存说明文档、可复用代码、实验入口、图片资源、运行日志和测试代码。文件多起来以后，如果全部堆在一起，很快就会找不到需要的内容。

所以，阅读项目的第一步不是立即打开最长的 Python 文件，而是先回答三个问题：

1. 项目从哪里开始？
2. 不同文件夹分别负责什么？
3. 哪些是源文件，哪些是运行后自动生成的文件？

这份教程只介绍整体结构，不要求现在读懂每个 `.py` 或 `.ipynb`。

---

## 2. 先看项目全貌

当前课程项目的简化结构如下：

```text
2026DeeplearningDemo/                 ← 项目根目录
├── README.md                         ← 整个项目的总说明
├── requirements.txt                 ← Python 依赖列表
├── .gitignore                       ← 告诉 Git 哪些生成文件不用管理
├── .vscode/                         ← VS Code 的项目设置
│
├── 01_mlp_lab/                      ← MLP 实验室
│   ├── README.md                    ← MLP 实验室说明
│   ├── 00_...ipynb ～ 06_...ipynb   ← 分阶段 Notebook
│   ├── mlp_lab/                     ← MLP 可复用代码包
│   ├── assets/                      ← 静态资源
│   ├── logs/                        ← 运行后生成的实验日志
│   ├── demo.py                      ← 命令行实验入口
│   └── test_...py                   ← 快速检查代码
│
├── 02_cnn_lab/                      ← CNN 实验室
│   └── cnn_lab/                     ← CNN 可复用代码包
├── 03_rnn_lab/                      ← RNN 实验室
│   └── rnn_lab/                     ← RNN 可复用代码包
├── 04_transformer_lab/              ← Transformer 实验室
│   └── transformer_lab/             ← Transformer 可复用代码包
│
├── check_environment.py             ← Python 与课程依赖检查入口
├── check_server_gpu_health.py       ← Linux 多 GPU 服务器只读诊断入口
├── seven_gpu_test_demo.py           ← 剩余七张 GPU 的课程可用性验收入口
└── 其他 .md / .pdf                  ← 安装与使用补充材料
```

这不是仓库的全部文件，而是一张“地图”。看懂地图后，再进入某个实验室，就不容易迷路。

### 什么是项目根目录

最外层的 `2026DeeplearningDemo` 叫作项目根目录。根目录中的路径可以作为共同起点。

例如：

```text
01_mlp_lab/03_训练第一个MLP.ipynb
```

它表示：从项目根目录进入 `01_mlp_lab` 文件夹，再找到对应的 Notebook。

这种从共同起点写出的路径叫作相对路径。项目代码尽量使用相对路径，换一台电脑或换一个学生账号后仍然容易运行。

---

## 3. 四个实验室文件夹

项目按照模型类型分成四个实验室：

| 文件夹 | 学习主题 | 主要数据形式 |
|---|---|---|
| `01_mlp_lab` | 多层感知机 MLP | 二维点、表格特征 |
| `02_cnn_lab` | 卷积神经网络 CNN | 图片与四维张量 |
| `03_rnn_lab` | RNN、LSTM、GRU | 序列与时间步 |
| `04_transformer_lab` | Transformer 与 Attention | Token 序列 |

每个实验室都尽量保持相似的组织方式：

```text
Notebook：负责讲解、交互、展示图和完成练习
demo.py：负责从终端快速运行完整实验
模型名_lab/：负责保存可以反复调用的核心代码
README.md：负责说明该实验室怎样使用
test_*.py：负责快速检查关键功能能否运行
```

这种相似性很重要。学会 MLP 项目的结构后，再进入 CNN、RNN 和 Transformer 项目，就能根据同样的规律寻找内容。

### 为什么还要有 `mlp_lab`、`cnn_lab` 这样的内层文件夹

外层的 `01_mlp_lab` 是一间完整“实验室”，里面既有课程、文档，也有代码。

内层的 `mlp_lab` 是一个 Python 代码包，集中保存数据处理、模型、训练和绘图等可复用逻辑。Notebook 和 `demo.py` 都可以调用它，避免在每份 Notebook 中复制同一大段代码。

这里通常还能看到 `__init__.py`。它帮助 Python 把这个目录识别为可以导入的代码包。例如：

```python
from mlp_lab import run_experiment
```

现阶段不需要逐个阅读包中的所有 `.py` 文件。先通过 Notebook 使用它们，等需要修改模型结构时再进入代码包。

---

## 4. `assets`：项目需要的静态资源

`assets` 可以翻译为“资源”。它通常保存程序需要使用、但本身不是 Python 代码的文件，例如：

- 字体；
- 图标；
- 示例图片；
- 配色文件；
- 小型演示素材。

当前项目中的主要内容是：

```text
01_mlp_lab/assets/fonts/
├── NotoSansCJKsc-Regular.otf
├── LICENSE-Noto-CJK.txt
└── README.md
```

它们分别表示：

- `.otf`：用于 Matplotlib 中文绘图的字体文件；
- `LICENSE`：字体的开源许可说明；
- `README.md`：这份资源自己的来源与使用说明。

当前 CNN、RNN 和 Transformer 的绘图代码也会复用这里的中文字体，因此没有必要在四个实验室中各复制一份十几 MB 的相同文件。

`assets` 中的资源通常随项目一起下载，不是每次运行实验后生成的。不要随意删除、改名或替换，否则程序可能找不到字体或图片。

---

## 5. `logs`：实验留下的记录

`log` 是“日志”，可以把它理解为实验的自动记录本。

当前项目中，MLP 的 `demo.py` 提供了最完整的自动日志示例。完成一次训练后，程序会自动创建 `01_mlp_lab/logs`，并保存：

```text
logs/
├── 时间_实验名.json
├── 时间_实验名.txt
└── history.csv
```

三种文件各有用途：

| 文件 | 保存什么 | 适合怎样查看 |
|---|---|---|
| `.json` | 参数、环境、源码哈希、指标和完整训练曲线 | 程序读取或详细追查 |
| `.txt` | 与终端相近的简短结果摘要 | 人直接阅读 |
| `history.csv` | 每次配置的一行汇总 | Excel、表格软件或 Python 对比 |

### 日志是如何生成的

进入 MLP 实验室并运行一次基线实验：

```bash
cd "$HOME/courses/Deeplearning2026/01_mlp_lab"
python demo.py --experiment baseline
```

代码内部会先检查 `logs` 是否存在；如果不存在，就自动创建，然后写入本次实验记录。

查看生成结果：

```bash
ls -lh logs
```

如果只是临时演示、不想写日志，可以运行：

```bash
python demo.py --experiment baseline --no-log
```

### 为什么需要日志

只看终端输出，窗口关闭后结果就很难找回。日志可以回答：

- 当时用了什么参数？
- 用的是 CPU 还是哪块 GPU？
- 最终指标是多少？
- 这次结果由哪个版本的代码产生？
- 与上一次实验相比，究竟改变了什么？

`logs` 是运行产物，不是课程源码，因此已经被 `.gitignore` 忽略。刚克隆项目时可能看不到这个目录，这是正常的；完成相应实验后它才会出现。

---

## 6. `outputs`：程序生成的图片或其他结果

`outputs` 通常保存程序导出的图像或结果文件。例如，在 MLP 实验室运行：

```bash
python demo.py --experiment gallery --save-dir outputs
```

程序会自动创建 `outputs`，并保存数据图。

简单区分：

```text
assets  → 程序运行前已经准备好的输入资源
outputs → 程序运行后生成的输出结果
logs    → 对运行过程和实验配置的记录
```

`outputs` 也被 `.gitignore` 忽略，所以每名学生可以生成自己的图片，而不会让 Git 把大量临时结果当成课程代码。

---

## 7. `__pycache__`：Python 自动生成的缓存

运行或导入 Python 代码后，目录中可能突然出现：

```text
__pycache__/
└── model.cpython-311.pyc
```

这不是病毒，也不是同学误建的文件夹。

Python 会把部分源代码转换成更方便下次加载的字节码，并缓存在 `__pycache__` 中。文件名中的 `cpython-311` 表示它由 CPython 3.11 生成。

需要记住四点：

1. 它由 Python 自动生成；
2. 不需要手工编辑；
3. 删除后通常不影响源代码，Python 下次运行时还会重新生成；
4. 不应该当作作业提交，也不需要上传到 Git 仓库。

所以项目的 `.gitignore` 中有：

```gitignore
__pycache__/
*.py[cod]
```

第一行忽略缓存目录，第二行忽略 `.pyc` 等编译缓存文件。

---

## 8. `.ipynb_checkpoints`：Jupyter 自动保存的检查点

编辑 Notebook 时，Jupyter 可能创建隐藏目录：

```text
.ipynb_checkpoints/
```

它保存自动检查点，用于部分恢复场景。它不是正式作业，也不是可靠的长期备份，因此同样被 Git 忽略。

正式实验仍然要保存自己的 Notebook，并按照[《保存个人实验并安全更新课程代码》](./2.2%28补充材料%29保存个人实验并安全更新课程代码.md)中的方法另外备份。

---

## 9. `.vscode` 和 `.git`

### `.vscode`

`.vscode` 保存这个项目在 VS Code 中使用的工作区设置。例如，它可以帮助统一文件编码、Notebook 行为或编辑器提示。

它是项目配置的一部分，通常由项目维护者管理。初学阶段不需要修改。

### `.git`

执行 `git clone` 后，项目根目录中会有隐藏的 `.git` 文件夹。它保存：

- Git 版本历史；
- 当前分支；
- 远程仓库地址；
- Git 内部状态。

`.git` 相当于这个项目的“版本管理档案室”。不要手工修改或删除它。删除后，普通文件可能仍在，但该目录将不再是一个正常的 Git 仓库，也就不能正常 `git pull`。

以英文句点开头的文件或目录通常是隐藏项。在 Linux 中可以这样查看：

```bash
ls -la
```

---

## 10. 常见文件类型

不需要记住每个具体文件，只要先认识它们承担的角色。

| 名称或后缀 | 含义 | 在本项目中的作用 |
|---|---|---|
| `README.md` | 项目说明书 | 介绍目的、结构、使用顺序和命令 |
| `.md` | Markdown 文档 | 教程、使用说明、作业要求 |
| `.py` | Python 源代码 | 数据、模型、训练、绘图或工具脚本 |
| `.ipynb` | Jupyter Notebook | 代码、文字、公式、图和输出放在同一页面 |
| `.pdf` | 固定版式文档 | 安装教程或补充材料 |
| `requirements.txt` | Python 依赖清单 | 记录项目通常需要哪些第三方库 |
| `.gitignore` | Git 忽略规则 | 排除缓存、日志、输出和个人作业文件 |
| `test_*.py` | 快速测试 | 检查关键功能是否还能正常运行 |
| `.ps1` | PowerShell 脚本 | 在 Windows 中执行环境检查等任务 |

### 什么是 README

`README` 可以理解为“请先读我”。GitHub 打开一个仓库时，会自动展示根目录中的 `README.md`。

本项目有不同层级的 README：

```text
根目录 README.md          → 介绍整个课程项目
01_mlp_lab/README.md      → 介绍 MLP 实验室
assets/fonts/README.md    → 介绍字体资源
```

这体现了一个重要习惯：说明文档应尽量放在它所解释的内容附近。

当你第一次拿到一个陌生项目时，推荐顺序是：

1. 先读根目录 `README.md`；
2. 再进入目标文件夹读它的 `README.md`；
3. 按说明找到程序入口；
4. 最后再阅读需要修改的源代码。

---

## 11. 哪些文件来自仓库，哪些由运行生成

下面这张流程图最值得记住：

```text
git clone / git pull
        │
        ├── 得到课程源码：.py、.ipynb
        ├── 得到说明文档：README、其他 .md、.pdf
        ├── 得到项目配置：.gitignore、.vscode、requirements.txt
        └── 得到静态资源：assets
                    │
                    ▼
              运行 Python / Notebook
                    │
        ┌───────────┼────────────┐
        ▼           ▼            ▼
   __pycache__    logs        outputs
   Python 缓存    实验记录     图片等结果
```

可以把项目内容分成四类：

| 类别 | 例子 | 是否通常由学生修改 |
|---|---|---|
| 源码与课程 | `.py`、官方 `.ipynb` | 按课程要求修改个人副本 |
| 文档与配置 | `README.md`、`.gitignore`、`.vscode` | 初学阶段通常只读 |
| 静态资源 | `assets` | 通常只读 |
| 生成内容 | `logs`、`outputs`、`__pycache__` | 程序自动产生，不改缓存 |

---

## 12. 用命令观察项目，而不是盲目点击

先进入项目根目录：

```bash
cd "$HOME/courses/Deeplearning2026"
```

查看根目录，包括隐藏项：

```bash
ls -la
```

查看两层以内的文件夹：

```bash
find . -maxdepth 2 -type d | sort
```

查看一个实验室：

```bash
ls -lh 01_mlp_lab
```

检查哪些文件是 Git 管理的课程文件：

```bash
git ls-files
```

检查本地是否修改了课程文件：

```bash
git status --short --branch
```

查看被忽略的缓存和输出时，可以使用：

```bash
git status --short --ignored
```

输出中以 `!!` 开头的项目是被 Git 忽略的内容，不表示程序出错。

---

## 13. 一个推荐的项目阅读顺序

第一次进入任何一个实验室时，可以按下面的顺序：

```text
第 1 步：阅读根目录 README，知道整个项目做什么
    ↓
第 2 步：阅读实验室 README，知道该实验怎样运行
    ↓
第 3 步：从 00_一键体验.ipynb 开始看到完整结果
    ↓
第 4 步：按照编号继续学习，不必先读全部底层代码
    ↓
第 5 步：复制 05_自主修改练习.ipynb，形成个人作业
    ↓
第 6 步：记录配置、图、指标和结论
    ↓
第 7 步：需要改变模型时，再进入模型代码包
```

这也是工程化学习的一部分：先找到入口和边界，再进入细节。

---

## 14. 什么是工程化思维

工程化不等于“文件越多越专业”，也不等于“必须使用复杂框架”。

对本课程来说，工程化主要表示：

- **职责清楚**：数据、模型、训练、绘图、文档和结果各有位置；
- **入口明确**：别人知道先读哪个 README、运行哪个 Notebook 或命令；
- **过程可记录**：参数、环境、代码版本和结果能够被日志保存；
- **实验可比较**：修改一项后，可以与基线进行公平对照；
- **结果可复现**：过一段时间仍能说清代码、参数和环境；
- **协作更安全**：课程源码、教师更新和学生个人文件不互相覆盖；
- **问题容易定位**：出错时能判断是环境、数据、模型、训练还是绘图环节。

一个只有几十行的课堂练习可以写在单个文件中；当项目需要多次实验、多人使用和持续更新时，就应该逐渐分离不同职责。

好的工程结构最终不是为了“看起来复杂”，而是为了让项目更容易理解、运行、修改、检查和交接。

### 最后的简单讨论

可以和同学讨论下面三个问题：

1. 如果所有数据、模型、训练、绘图和结果都放在一个 Python 文件里，修改时可能遇到什么问题？
2. 如果没有 README 和日志，另一名同学能否重复你的实验？
3. `assets`、`logs` 和 `__pycache__` 都不是模型代码，为什么还要区分它们？

记住本节最重要的一句话：

> **工程化不是把项目变复杂，而是把已经存在的复杂性放到正确的位置。**
