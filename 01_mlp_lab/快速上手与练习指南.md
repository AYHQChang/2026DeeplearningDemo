# MLP 实验快速上手与练习指南

## 1. 先运行，再理解

第一次进入实验时，不要求阅读训练循环或绘图代码。按顺序打开 5 份 notebook，
每完成一步都会得到图片或数值结果。

```text
00_一键体验
    ↓
01_认识数据与张量
    ↓
02_训练第一个MLP
    ↓
03_组件对比实验
    ↓
04_自主修改练习
```

## 2. 五分钟开始

1. 打开 `00_一键体验.ipynb`；
2. 选择课程使用的 Python 内核；
3. 点击 `Run All`；
4. 等待结果表和两张图片出现；
5. 先观察结果，不需要立即理解隐藏的准备单元。

默认设备为：

```python
device="auto"
```

`auto` 会在 CUDA 可用时使用 GPU，否则使用 CPU。显式选择设备时可填写：

```python
device="cpu"
device="cuda"
```

## 3. 每份 notebook 要完成什么

### 00｜一键体验

运行一行 `quick_demo(...)`，得到：

- 训练阶段决策边界；
- 最终边界与误判点；
- Loss 曲线；
- 训练集与测试集 Accuracy；
- Gradient norm；
- 参数量与训练时间。

### 01｜认识数据与张量

查看月牙、同心圆和螺旋三种数据，确认：

- `X` 的形状为 `[样本数, 2]`；
- `y` 的形状为 `[样本数]`；
- 训练集与测试集分开；
- 标准化参数只从训练集估计。

### 02｜训练第一个 MLP

只训练一个模型，按以下顺序读图：

1. 决策边界是否逐渐稳定；
2. Loss 是否总体下降；
3. 测试集 Accuracy 是否跟随训练集变化；
4. 误判点主要出现在哪里；
5. 梯度是否过大、过小或剧烈震荡。

### 03｜组件对比实验

每次只替换一个组件。默认提供激活函数与优化器两组对比，每列对应一种配置。
不能只根据最高 Accuracy 下结论，还要结合边界、Loss、泛化差距和梯度。

### 04｜自主修改练习

参数区只需要修改一行：

```python
hidden_sizes = (16, 16)
activation = "tanh"
optimizer = "adam"
dropout = 0.0
learning_rate = 0.02
```

每次只改一个变量，运行后立即比较基线图和修改后的图。

## 4. 从参数修改进入代码修改

完成 5 份 notebook 后，再阅读 `mlp_lab/model.py`。该文件只包含激活函数映射和
MLP 结构，适合完成以下练习：

| 目标 | 修改位置 | 建议改动量 |
|---|---|---:|
| 增加 LeakyReLU 或 ELU | `mlp_lab/model.py` 的 `_activation()` | 1 行 |
| 调整 Linear、Activation、Dropout 顺序 | `mlp_lab/model.py` 的 `MLP` | 2–5 行 |
| 增加 RMSprop | `mlp_lab/engine.py` 的验证与优化器函数 | 2–3 行 |
| 增加新的损失方案 | `mlp_lab/engine.py` 的损失函数 | 2–5 行 |

数据生成、日志和绘图代码不属于首次修改范围。

## 5. 实验记录

| 实验编号 | 我的预测 | 唯一修改项 | seed | 参数量 | 基线结果 | 新结果 | 图形证据 | 结论 |
|---|---|---|---:|---:|---:|---:|---|---|
| 1 |  |  | 42 |  |  |  |  |  |
| 2 |  |  | 42 |  |  |  |  |  |
| 3 |  |  | 42 |  |  |  |  |  |

建议使用以下句式：

> 本次只修改了 ______。从 ______ 图可以看到 ______。在当前数据、随机种子和训练轮数下，结果支持 / 不支持 ______。

避免写成“某组件永远更好”。结论只适用于当前实验条件。

## 6. 完成标准

一次完整练习至少包含：

1. 一次成功运行和对应图片；
2. 一条只改变一个变量的对照；
3. 一项数值证据和一项图形证据；
4. 一段说明结论边界的文字；
5. 修改模型代码时，通过 `python test_mlp_smoke.py`。

需要查询全部命令行参数、日志文件和图形含义时，再打开 `使用手册.md`。
