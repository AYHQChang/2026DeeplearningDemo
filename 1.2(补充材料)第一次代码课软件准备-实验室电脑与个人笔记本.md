# 第一次代码课软件准备：实验室电脑与个人笔记本

## 1. 为什么要分成两条路线

第一次代码课中发现了两个实际问题：

1. 实验室电脑带有还原功能，重新开机后，临时安装的软件或扩展可能消失；
2. 个人笔记本上的新版 VS Code 无法连接当前课程服务器，需要使用课堂验证过的 VS Code 1.98.2。

因此，实验室电脑和个人笔记本不能照着同一套步骤操作。

```text
实验室电脑
    └── 打开已经安装的 VS Code
        └── 检查并补装扩展
            └── 连接服务器

个人笔记本
    └── 解压课程专用 VS Code 1.98.2
        ├── 安装并保留扩展
        ├── 安装 Miniconda
        ├── 安装 Windows Git
        └── 连接服务器或在本地运行代码
```

安装前先检查。已经安装并且能正常使用的内容，不要重复安装。

---

## 2. 本课程需要哪些软件和扩展

### VS Code 扩展

| 名称 | 发布者或扩展 ID | 用途 |
|---|---|---|
| Remote - SSH | Microsoft；`ms-vscode-remote.remote-ssh` | 从 Windows 连接 Linux 服务器 |
| Python | Microsoft；`ms-python.python` | Python 语法、解释器选择和调试 |
| Jupyter | Microsoft；`ms-toolsai.jupyter` | 打开和运行 `.ipynb` Notebook |
| Markdown Preview Enhanced | `shd101wyy.markdown-preview-enhanced` | 预览课程 Markdown 文档 |

> Git 不是 VS Code 扩展，而是一套独立的版本管理软件。

### 两类电脑的安装要求

| 内容 | 实验室电脑 | 个人笔记本 |
|---|---|---|
| VS Code | 打开电脑中已经安装的版本 | 使用班长发放的 1.98.2 ZIP 版 |
| 四个 VS Code 扩展 | 缺哪个装哪个；电脑还原后重新检查 | 安装一次并保存在便携版 `data` 中 |
| Windows Miniconda | 不安装，使用服务器环境 | 安装并创建课程环境 |
| Windows Git | 不安装，服务器已经有 Git | 安装一次，供本地项目使用 |
| Linux 服务器 Git | 服务器已经安装 | 远程连接后直接使用 |

---

## 3. 路线 A：使用实验室电脑

### 第 1 步：打开已经安装的 VS Code

点击 Windows 开始菜单，在搜索框输入：

```text
Visual Studio Code
```

打开已经安装的软件，不在网页中重新下载 VS Code。

### 第 2 步：检查扩展

点击左侧四个小方块组成的“扩展”图标，或者按：

```text
Ctrl + Shift + X
```

依次搜索本教程第 2 节列出的四个扩展。

判断方法：

- 显示“已安装”或齿轮按钮：不需要重复安装；
- 显示“安装”：点击安装；
- 出现多个同名结果：核对发布者和扩展 ID，不安装来历不明的同名扩展。

电脑带有还原功能。下一次开机后仍然先检查，不要凭上节课的状态判断。

### 第 3 步：先检查 Windows SSH 客户端

打开 PowerShell：

```powershell
ssh -V
```

成功时会看到类似：

```text
OpenSSH_for_Windows_9.xp1, LibreSSL ...
```

如果提示无法识别 `ssh`，停止操作并联系教师，不要临时下载第三方 SSH 软件。

### 第 4 步：连接服务器

1. 按 `F1` 或 `Ctrl + Shift + P`；
2. 输入并选择 `Remote-SSH: Connect to Host...`；
3. 选择课堂提供的服务器；
4. 按提示输入自己的账号和密码；
5. 查看 VS Code 左下角，确认已经显示远程主机名称。

详细连接和故障排查见[《VS Code Remote-SSH 连接 Linux 服务器与失败排查》](./1.1%28补充材料%29VSCode%20Remote-SSH连接Linux服务器与失败排查.md)。

### 第 5 步：在远程窗口检查扩展

连接服务器后，再打开扩展页面。Python 或 Jupyter 旁边可能显示：

```text
Install in SSH: 服务器名称
```

需要运行远程 Notebook 时，点击这个按钮。它表示把扩展安装到自己的服务器环境，不是再次安装整个 VS Code。

### 第 6 步：检查服务器 Git

一定要在 VS Code 的远程终端中运行：

```bash
git --version
```

能看到 `git version ...` 就说明服务器 Git 可用。实验室电脑不需要另外安装 Windows Git。

### 实验室电脑每次上课速查

```text
□ 搜索并打开已安装的 VS Code
□ 检查 Remote - SSH 是否还在
□ 检查 ssh -V
□ 连接自己的服务器账号
□ 确认左下角显示 SSH 远程主机
□ 在远程终端检查 git --version
□ 打开自己的课程目录
```

---

## 4. 路线 B：使用个人笔记本

### 第 1 步：保留原来的新版 VS Code

不需要卸载笔记本原有的新版 VS Code。新版可以继续用于其他项目，课程专用的 1.98.2 可以与它并存。

### 第 2 步：解压课程专用 VS Code

使用班长发放的：

```text
VSCode-win32-x64-1.98.2.zip
```

将它解压到只含英文字母、数字和常见英文符号的路径，例如：

```text
D:\DLTools\VSCode-1.98.2
```

不要放到这些位置：

```text
D:\张三\深度学习软件\
C:\Users\中文姓名\Desktop\课程软件\
```

路径中避免中文、全角符号和过长目录名。

### 第 3 步：启用便携模式

在 `Code.exe` 同一层新建一个空文件夹，名称必须是：

```text
data
```

正确结构：

```text
D:\DLTools\VSCode-1.98.2\
├── Code.exe
├── data\
└── bin\
```

`data` 会保存这一份 VS Code 的设置、扩展和使用状态。以后仍然打开这里的 `Code.exe`，不需要反复安装扩展。

### 第 4 步：创建桌面快捷方式

右键 `Code.exe`，选择：

```text
发送到 → 桌面快捷方式
```

建议把快捷方式改名为：

```text
深度学习课程 VS Code 1.98.2
```

这样不会与笔记本原有的新版 VS Code 混淆。

### 第 5 步：安装四个扩展

打开课程专用 VS Code，按 `Ctrl + Shift + X`，依次安装：

```text
Remote - SSH
Python
Jupyter
Markdown Preview Enhanced
```

安装完成后关闭 VS Code，再从“深度学习课程 VS Code 1.98.2”快捷方式重新打开，检查扩展仍然显示已安装。

如果扩展市场提示当前版本不兼容，不要升级 VS Code。记录扩展名称和完整提示，联系教师领取与 1.98.2 匹配的 VSIX 离线安装包。

### 第 6 步：安装笔记本本地工具

按照下面两份独立教程操作：

1. [Windows 笔记本安装与配置 Miniconda](./1.3%28补充材料%29Windows笔记本安装与配置Miniconda.md)
2. [Windows 笔记本安装与配置 Git](./1.4%28补充材料%29Windows笔记本安装与配置Git.md)

已经安装并通过验证的 Miniconda 或 Git 不需要重复安装。

### 第 7 步：连接服务器

使用课程专用 VS Code 1.98.2，按照 Remote-SSH 教程连接。连接成功后，在远程终端运行：

```bash
git --version
python --version
```

这里显示的是服务器上的 Git 和 Python，与笔记本本地安装的软件是两套环境。

### 个人笔记本课前速查

在本地 PowerShell 中检查：

```powershell
ssh -V
conda --version
git --version
```

然后检查：

```text
□ 从不含中文的路径打开 VS Code 1.98.2
□ `data` 文件夹存在
□ 四个扩展已安装
□ Miniconda 安装验证通过
□ Git 安装验证通过
□ Remote-SSH 可以连接服务器
```

---

## 5. 怎样区分本地终端和远程终端

这是初学者最容易混淆的地方。

### Windows 本地 PowerShell

提示符通常类似：

```text
PS C:\Users\你的用户名>
```

这里运行的是笔记本本地安装的 Conda、Git 和 Python。

### Linux 服务器终端

提示符通常类似：

```text
你的服务器用户名@服务器名:~$
```

这里运行的是服务器上的 Conda、Git、Python 和课程代码。

执行安装或环境配置前，先看提示符，确认自己正在哪台电脑上操作。

---

## 6. 课堂停止条件

出现下面情况时不要反复点击安装，也不要随意复制网上的命令：

- VS Code 不是 1.98.2，且无法连接课程服务器；
- 解压路径包含中文，程序启动或扩展安装异常；
- 扩展提示与当前 VS Code 不兼容；
- `ssh -V` 无法执行；
- 不清楚当前是 Windows 本地终端还是 Linux 远程终端；
- 安装器要求管理员权限或安全软件报告异常；
- `conda`、`git` 或 `python` 指向的路径与教程不一致。

保留完整报错文字或截图，再联系教师处理。不要用卸载系统软件、关闭安全功能、重装系统等方式解决课堂配置问题。

---

## 7. 官方参考

- [VS Code Portable Mode](https://code.visualstudio.com/docs/setup/portable)
- [VS Code Remote Development using SSH](https://code.visualstudio.com/docs/remote/ssh)
